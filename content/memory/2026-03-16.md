## New2005 Finance moved from fixture-only intelligence to a live adapter path

- Action taken:
  - added a shared Financial Datasets client/cache at `projects/new2005-finance/src/lib/financial-datasets-client.ts`
  - wired `projects/new2005-finance/src/lib/financial-datasets.ts` to return real stock overview data when `FINANCIAL_DATASETS_API_KEY` is present, while preserving demo fallback
  - added `projects/new2005-finance/src/lib/market-intelligence-adapter.ts` to synthesize `market-intel.v1` from live official data, live news, and explicitly derived community-style signals
  - updated the intelligence panel copy so it reports `Live contract` when provider-backed evidence is flowing instead of falsely claiming demo-only mode
- Result/evidence:
  - `npm run lint` passed in `projects/new2005-finance`
  - `npm run build` passed in `projects/new2005-finance`
  - README now reflects the true posture: adapter-driven with live provider access, demo fallback otherwise
- Operational ACPX state rechecked:
  - `openclaw config get plugins.entries.acpx.config.permissionMode` returned `approve-all`
  - `openclaw config get plugins.entries.acpx.config.nonInteractivePermissions` returned `fail`
  - `scripts/openclaw-readiness.sh` still reports `acpx_permission = ok`
- Guardrail learned:
  - once ACPX host permissions are open, the next failure mode is often stale app plumbing rather than runtime permission. Fix the actual execution path instead of re-litigating permissions.

## OpenClaw control-plane routing and audit layer tightened

- Action taken:
  - fixed `scripts/openclaw-session-spawn-audit.sh` so its `rg` exclusions no longer produce false audit noise
  - updated `scripts/openclaw-telegram-acp-audit.sh` so missing reload-log lines no longer incorrectly count as hard runtime failure when ACPX backend, gateway, and Telegram provider are actually healthy
  - updated `scripts/openclaw-capability-audit.sh` so `im_channels` stays partial until Telegram thread closure is verified, and `acp_orchestration` stays partial while autonomy health is degraded
  - updated `scripts/openclaw-autonomy-guard.sh` to distinguish stale/hard-degraded session evidence from fresh fallback evidence
  - updated `scripts/openclaw-complex-cycle.sh` to emit explicit ACP routing artifacts for implementation and Codex review instead of leaving the routing choice implicit
- Verified result:
  - `bash scripts/openclaw-session-spawn-audit.sh` now runs cleanly without the prior `rg: -g: No such file or directory` noise
  - `python3 scripts/openclaw-telegram-acp-audit.sh` now reports `runtime_ready=true` while still correctly keeping `overall=partial` because `thread_spawn_verified=false` and `live_conversation_bindable=false`
  - `scripts/openclaw-capability-audit.sh` now reports `acp_orchestration.status=partial` instead of incorrectly calling it fully ready while autonomy is degraded
  - `scripts/projectctl.sh acp-dispatch recommend ...` still gives the correct split:
    - host `claude-code` => bounded run
    - host `codex --prefer-session` => persistent session
    - telegram `codex --prefer-session` => bounded-run fallback
- Guardrail learned:
  - the current OpenClaw bottleneck is no longer ACPX write permission. It is control-plane honesty: route by surface, treat Telegram plain groups as bounded-only, and do not let stale or overly strict audits misclassify the runtime.

## Autonomy session evidence now resolves from real local transcripts instead of stale registry entries

- Root cause found:
  - `scripts/openclaw-session-evidence.sh` only trusted `~/.openclaw/sessions/sessions.json`
  - that registry does not contain the current autonomy keys like `agent:goal-manager-auto:main` and `agent:evolution-auto:main`
  - result: the audit fell through to gateway `sessions.get` or `world_state`, then reported false `session evidence degraded` even though real transcript files already existed under `~/.openclaw/agents/*/sessions/`
- Action taken:
  - patched `scripts/openclaw-session-evidence.sh` to:
    - discover agent session files directly from `~/.openclaw/agents/<agent>/sessions/`
    - use `world_state` `agent_session_id` when available to select the exact transcript file
    - fall back to the newest agent session transcript when the legacy registry is stale
    - report `message_count` from actual message records instead of raw line count
    - mark local-only truth as `partial` instead of forcing `degraded` whenever the source is not gateway
  - patched `scripts/openclaw-autonomy-guard.sh` so stale local evidence is classified as `partial`, not mixed into hard degradation
- Verified result:
  - `python3 scripts/openclaw-session-evidence.sh autonomy 5` now returns local transcript evidence for all four autonomy keys:
    - `reviewer-auto` via `agent_sessions_dir_latest:reviewer-auto`
    - `world-model` via `agent_sessions_dir_latest:world-model`
    - `goal-manager-auto` via `world_state_session_id`
    - `evolution-auto` via `agent_sessions_dir_latest:evolution-auto`
  - `python3 scripts/openclaw-autonomy-guard.sh 5` now reports:
    - `overall=partial`
    - no `degraded_agents`
    - only `stale session evidence ignored: evolution-auto, goal-manager-auto, reviewer-auto, world-model`
- Guardrail learned:
  - for OpenClaw autonomy audits, stale local transcripts are still better than pretending evidence is missing; registry drift and runtime freshness are separate defects and must stay separate in the control plane.

## AGI runtime freshness restarted, and the main remaining bottleneck is scheduler/agent unevenness rather than permissions

- Root cause found after the session-evidence repair:
  - gateway was alive, but the AGI workers were not:
    - `~/.openclaw/state/autonomous-loop.pid` missing
    - `~/.openclaw/state/learning-engine.pid` missing
    - `~/.openclaw/state/world-state.json` had stalled on older work
  - after restarting AGI, two more control bugs became explicit:
    - `run-agent-safe.sh` rejected valid current agent outputs because it only validated the old `{status, summary, result}` envelope and did not accept the live `{payloads, meta}` shape
    - `~/.openclaw/core/autonomous-loop.ts` kept reselecting the P0 failure-analysis goal even when it would immediately hit reviewer cooldown and do zero real work
- Action taken:
  - restarted AGI successfully with `~/.openclaw/scripts/start-agi.sh`
  - patched `~/.openclaw/scripts/run-agent-safe.sh` so successful `{payloads, meta}` outputs with non-empty payload text and `meta.stopReason=stop` are accepted instead of being retried/misclassified
  - patched `~/.openclaw/core/autonomous-loop.ts` twice:
    - `recentFailures` now uses a real time window (`OPENCLAW_RECENT_FAILURE_WINDOW_MS`, default 6h) instead of blindly treating the last 12 historical entries as “recent”
    - goal selection now bypasses a goal that would only hit task cooldown, instead of burning a full loop on `reviewer -> Skipped by control guard`
- Verified result:
  - after the runtime restart, `autonomous-loop.out` and `world-state.json` became live again
  - after the scheduler patch, the loop stopped selecting the zero-work reviewer cooldown path and switched to:
    - `Selected goal: Reflect on recent cycles and convert lessons into stronger OpenClaw brain functions (P1)`
    - `Executing 3 task(s)`
  - fresh autonomy evidence now shows:
    - `goal-manager-auto` refreshed to `last_message_at=2026-03-16T00:38:20.195Z`
    - `evolution-auto` refreshed to `last_message_at=2026-03-16T00:38:10.948Z`
    - `reviewer-auto` and `world-model` are still the lagging surfaces in the current pass
- Remaining bottleneck:
  - the problem is no longer ACPX permission, stale registry lookup, or pure scheduler starvation
- the remaining bottleneck is agent unevenness under the current runtime:
    - `reviewer-auto` and especially `world-model` still lag behind the refreshed `goal-manager-auto` and `evolution-auto`
    - some agent runs are still vulnerable to gateway restart / embedded timeout turbulence during long runs

## Bootstrap memory compaction and context audit are now first-class controls

- Root cause confirmed:
  - the main workspace `MEMORY.md` had grown into long-form history and doctrine, making it the highest-risk bootstrap input for truncation and degraded continuity
  - the issue was not ACPX permission anymore; it was bootstrap inefficiency
- Action taken:
  - archived long-form memory separately and replaced `workspace/MEMORY.md` with a compact durable bootstrap version
  - updated `workspace/AGENTS.md` to explicitly enforce bootstrap-budget discipline: small `MEMORY.md`, raw logs in daily memory, bulky material in `refs/` or `docs/`
  - added `scripts/openclaw-context-audit.sh` plus `scripts/projectctl.sh context-audit` to measure bootstrap file sizes across the main workspace and all agent workspaces
  - set explicit bootstrap defaults in `openclaw.json`: `bootstrapMaxChars=12000`, `bootstrapTotalMaxChars=80000`
- Guardrail learned:
  - OpenClaw memory quality is not improved by dumping more history into bootstrap files; it improves when durable truth stays compact and everything bulky is routed to the right storage layer.

## Agent bootstrap duplication was compacted, and direct ACPX Codex exposed a new runtime bottleneck

- Direct ACPX attempt:
  - used plugin-local ACPX at `/opt/homebrew/lib/node_modules/openclaw/extensions/acpx/node_modules/.bin/acpx`
  - verified permission posture remained open enough: `approve-all` + `nonInteractivePermissions=fail`
  - created a named ACPX Codex session successfully, but both persistent-session prompt and one-shot `acpx codex exec` stalled before any prompt event or file mutation
  - stream evidence showed Codex ACP initialization and session creation, but no actual prompt dispatch; this is now a concrete ACPX/Codex runtime bottleneck, not a permissions narrative
- Optimization work completed locally after ACPX stall:
  - compacted specialist-agent `AGENTS.md` files for:
    - `goal-manager`, `world-model`, `evolution`, `reviewer`, `coder`, `tester`, `optimizer`, `architect`
  - tightened role `HEARTBEAT.md` files for the same agents so they keep role-critical instructions without generic workspace/chat boilerplate
- Measured gains:
  - each target agent `AGENTS.md` dropped from `7789` chars to about `1353-1389`
  - heartbeat reductions ranged from `307` to `616` chars
  - workspace totals dropped to:
    - `goal-manager`: `17700 -> 10651`
    - `world-model`: `17422 -> 10578`
    - `evolution`: `17414 -> 10559`
    - `reviewer`: `15661 -> 8863`
    - `coder`: `15256 -> 8493`
    - `tester`: `15959 -> 9765`
    - `optimizer`: `15884 -> 9158`
    - `architect`: `15876 -> 9121`
- Verification:
  - `python3 scripts/openclaw-context-audit.sh` now still reports:
    - `warningCount=0`
    - `memoryBloatCount=0`
    - `overPerFileLimitCount=0`
    - `overTotalLimitCount=0`
- Guardrail learned:
  - ACPX “session exists” is not the same as “prompt path is healthy.” The control plane should treat `session created but no prompt event / no file mutation` as a distinct degraded class.

## Shared specialist templates were compacted further, and ACPX diagnosis was corrected with a minimal direct repro

- ACPX diagnosis update:
  - a minimal direct repro succeeded: `acpx codex exec "Reply with exactly: ACPX_OK"` returned `ACPX_OK`
  - this means direct ACPX Codex prompting is functional in principle
  - the real ACPX bottleneck is narrower: long edit turns and session-style flows can spend too long in inspection or delayed execution, so the control plane must not treat ACPX dispatch as completed until file mutation or audit evidence appears
- Action taken:
  - compacted the shared specialist-agent templates across `goal-manager`, `world-model`, `evolution`, `reviewer`, `coder`, `tester`, `optimizer`, and `architect`:
    - `SOUL.md`
    - `BOOTSTRAP.md`
    - `IDENTITY.md`
    - `USER.md`
    - `TOOLS.md`
- Measured gains:
  - per specialist workspace, the shared-template stack dropped from `5042` chars to `1462`
  - across the 8 specialist agents, that shared-template stack dropped from `40336` to `11696`, saving `28640` chars
  - full specialist-workspace totals dropped from `77188` after the first compaction pass to `48548` after this shared-template pass
- Verification:
  - `python3 scripts/openclaw-context-audit.sh` still reports:
    - `warningCount=0`
    - `memoryBloatCount=0`
    - `overPerFileLimitCount=0`
    - `overTotalLimitCount=0`
- Guardrail learned:
  - ACPX can succeed on a minimal direct prompt while still being too slow or too opaque for long editing turns. For control-plane honesty, ACPX success must stay artifact-gated, not dispatch-gated.

## AibrowserAtlas production audit separated real usage from acceptance traffic

- Scope:
  - production host `96.44.158.158`
  - app data at `/srv/chat-mirror/server/data/app.db`
  - app logs via `docker compose logs`
- Concrete evidence:
  - total records are no longer zero: `users=162`, `sessions=165`, `messages=364`, `workspace_assets=363`
  - but the dataset is heavily acceptance-dominated: `159/162` users are `@example.com`, and `328/363` assets contain `acceptance` in the filename
  - real non-example users currently total `3`; over the last 24h, `2` new non-example users registered, but `0` non-example users produced messages or assets in that window
  - the only clear non-test artifact so far is one context-compression markdown file from the earlier Gmail user; there is not yet evidence of real-user finance/doc/ppt/website generation in production
- Product diagnosis:
  - backend generation is not the current blocker: asset status is overwhelmingly healthy (`361 ready`, `2 email_failed` from old export-email tests), with no visible 5xx generation wave
  - the real bottleneck is the front-door funnel: many homepage hits but almost no real auth/chat/generation depth
  - one concrete backend issue also remains: the SPA `setNotFoundHandler` currently serves `index.html` with HTTP 200 for arbitrary unknown paths, which makes scanner probes look “successful” and can feed security/phishing heuristics
- Guardrail learned:
  - production usage audits must always split `acceptance/test` traffic from real-user traffic first; otherwise a healthy test harness can masquerade as product adoption.

## Chat Mirror invite-only funnel got a stronger first-turn launch and safer 404 behavior

- Source files changed:
  - `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/src/App.tsx`
  - `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/index.ts`
  - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260315T203742Z.json`
- Concrete action taken:
  - kept the invite-only gate intact, but changed the post-register / empty-workspace behavior so a fresh user now lands with a selected capability and a prefilled starter brief in the composer instead of an entirely blank first turn
  - made the empty-state capability buttons operational: clicking a mode now fills a send-ready starter brief instead of only toggling visual selection
  - tightened the server SPA fallback so only real app routes (`/`, `/chat`, `/chat/...`) return `index.html`; suspicious probe paths and unknown config-like URLs now return real `404`
- Production evidence:
  - browser verification on `https://chat.aibrowseratlas.com/` with a fresh registration confirmed the composer was auto-seeded with a PPT starter brief immediately after entering the workspace, and the empty state exposed a direct `填入示例 brief` action
  - external verification confirmed `https://chat.aibrowseratlas.com/stripe_secret.json` now returns HTTP `404`, while `https://chat.aibrowseratlas.com/chat` remains HTTP `200`
  - fresh formal acceptance passed and wrote `projects/chat-mirror/data/acceptance/20260315T203742Z.json`, with `latest.json` updated to the same run
- Guardrail learned:
  - for invite-only products, the right move is not to remove the gate but to eliminate the blank-screen moment immediately after it; access friction and first-turn friction are separate problems and need separate fixes.

## OpenClaw runtime pathing and health probes were hardened to match the real host layout

- Root causes confirmed:
  - some direct ACPX instructions still assumed a repo-local binary path like `./extensions/acpx/node_modules/.bin/acpx`, but this host runs OpenClaw from a global install layout under `/opt/homebrew/lib/node_modules/openclaw`
  - the Claude health probe targeted `claude-haiku-4-5-20251001`, and the current proxy route mis-served that model request with a Codex-account compatibility error even while `claude-opus-4-6` worked
  - autonomy freshness defaults were too strict for the real loop profile; a single live reviewer/reflection pass could make healthy agents look stale after only 5 minutes
- Action taken:
  - added `scripts/openclaw-acpx-path.sh` to resolve ACPX from `ACPX_CMD`, npm global OpenClaw install, common host paths, or PATH
  - updated `scripts/openclaw-acpx-codex-smoke.sh` to use the resolver instead of a hardcoded absolute binary path
  - updated `scripts/claude-execution-policy.json` to probe `claude-opus-4-6` and stop treating the currently broken Haiku route as the readiness truth source
  - widened `scripts/openclaw-autonomy-guard.sh` freshness defaults to `idle=15m`, `active=30m`
  - documented the binary-resolution rule in `docs/ACP_ACPX_INTEGRATION_PROTOCOL.md`
- Verified result:
  - `scripts/openclaw-acpx-path.sh` resolved `/opt/homebrew/lib/node_modules/openclaw/extensions/acpx/node_modules/.bin/acpx`
  - `python3 scripts/openclaw-acpx-codex-smoke.sh` returned `overall=ok`
  - `scripts/claude-health.sh --json` returned `claude-opus-4-6 -> ok`
  - `python3 scripts/openclaw-autonomy-guard.sh 5` returned `overall=ok`
  - `bash scripts/openclaw-readiness.sh` returned `overall=ok`
  - `python3 scripts/openclaw-capability-audit.sh` now shows:
    - `runtime_health.status=ok`
    - `acp_orchestration.status=ready`
    - `im_channels.status=partial`
- Remaining truthful gap:
  - OpenClaw runtime is now healthy, but Telegram ACP closure is still only partially verified:
    - current live Telegram surface is bindable (`direct`)
    - direct Telegram-origin ACP thread-spawn closure still lacks explicit artifact proof
- Guardrail learned:
  - for OpenClaw health, use the host’s real execution path and the provider/model route that actually works; do not let a broken probe target or a fake local path downgrade the whole runtime.

## ACPX best-practice routing is now explicit: Telegram for intake, host-side ACPX for execution

- User decision captured:
  - stop over-focusing on Telegram ACP continuity
  - treat Telegram as the communication / requirement surface
  - make host-side ACPX the default execution plane
- Action taken:
  - added `scripts/openclaw-acpx.sh` as a best-practice wrapper around resolved ACPX execution
  - exposed it through `scripts/projectctl.sh`:
    - `acpx-path`
    - `acpx-exec <agent-id> <cwd> <prompt>`
    - `acpx-session <agent-id> <session-name> <cwd> <prompt>`
    - `acpx-status <agent-id> [session-name]`
  - updated capability/doctrine text so Telegram ACP thread closure is informational for IM channels, not the main execution bottleneck when host-side ACPX is healthy
- Operating rule going forward:
  - requirements come in over Telegram
  - durable multi-step coding runs through host-side ACPX sessions
  - Telegram thread-binding tests are only worth discussing when explicitly validating the IM channel itself

## Heartbeat cadence restored and verified in live runtime

- User intent captured:
  - keep OpenClaw and development work active without returning to noisy heartbeat spam
- Action taken:
  - restored moderate heartbeat scheduling in `~/.openclaw/openclaw.json`
  - kept active hours bounded to `09:00-23:00 Asia/Shanghai`
  - set cadence to:
  - `main`: `30m`
  - `architect`, `coder`, `tester`, `reviewer`, `optimizer`, `evolution`, `goal-manager`, `world-model`: `2h`
  - `mentor`: `6h`

## AibrowserAtlas unified left-rail session cards, desktop empty state, and message support rails

- Source files changed:
  - `projects/chat-mirror/web/src/App.tsx`
  - `projects/chat-mirror/web/src/styles.css`
- Concrete action taken:
  - reworked left-rail session cards so each session now reads like workspace navigation instead of a generic chat list: agent or auto-routing state appears as a compact presence line, the current session gets a stronger edge accent, and time/files were normalized into the same chip language
  - reshaped the desktop empty state into a clearer two-column layout: the left side focuses on the three simplest ways to start, while the right side keeps capability selection and starter actions in a contained studio block instead of spreading controls down the page
  - replaced the heavier assistant-message trace row with a lighter support rail that uses compact `sources / context / memory` pills, makes source/context panels mutually exclusive, and prevents mobile mis-taps from opening message actions when the user is interacting with evidence panels
- Verification:
  - `npm run build` passed in `projects/chat-mirror/web`
  - `npm run build` passed in `projects/chat-mirror/server`
  - deployed successfully to `https://chat.aibrowseratlas.com`
  - smoke check returned `ok` with version `1.0.1 / v1 / stable`
  - live browser verification confirmed:
    - the left rail shows the new session-card hierarchy and current-session emphasis
    - a fresh desktop session lands on the new two-column empty state
    - assistant replies now expose the lighter `上下文 3.4k` support rail and open the context ledger without triggering message-action noise
  - formal acceptance passed again and wrote `projects/chat-mirror/data/acceptance/20260316T073102Z.json`, with `latest.json` updated to the same run
- Guardrail learned:
  - product polish at this stage is mostly structural, not decorative: the highest-value changes are the ones that lower navigation and evidence friction without adding another layer of visible controls

## AibrowserAtlas de-duplicated the right rail overview and compressed the composer into summary mode

- Source files changed:
  - `projects/chat-mirror/web/src/App.tsx`
  - `projects/chat-mirror/web/src/styles.css`
- Concrete action taken:
  - removed duplicated file and memory lists from the right-rail `overview` tab so that total-view only keeps long-term status, context ledger, generation controls, and recent assets
  - moved recent memory access into the `library` tab, which keeps the quoting workflow available without forcing the main overview to carry another full section
  - tightened right-rail generation density by limiting quick alternate outputs to a smaller set and turning the `search / library` shortcuts into clearer counted nav buttons
  - simplified the desktop composer by removing redundant release metadata from the input toolbar and replacing the footer’s filename list with compact summary chips for current focus, memory scope, and attached-file count
- Production evidence:
  - deployment to `https://chat.aibrowseratlas.com` succeeded; live `/api/version` and host-side smoke both returned `1.0.1 / v1 / stable`
  - live browser verification on the public site confirmed:
    - the empty-state composer now lands with a compact summary footer (`记忆作用域 · 工作区记忆`) instead of a noisier status/file-name strip
    - the right rail `overview` now shows only summary cards plus `最近资产`, while `搜索资源` and `管理资料` are promoted as counted navigation buttons
  - formal acceptance passed again and wrote:
    - `projects/chat-mirror/data/acceptance/20260316T075150Z.json`
    - `projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - once a product already has dedicated tabs for search and library, the overview tab should stop acting like a second copy of them; density usually comes from duplicated surfaces, not from a lack of whitespace

## AibrowserAtlas mobile workspace now uses a single attachment entry and a collapsed right-rail tools strip

- Source files changed:
  - `projects/chat-mirror/web/src/App.tsx`
  - `projects/chat-mirror/web/src/styles.css`
- Concrete action taken:
  - replaced the mobile composer’s separate file and folder buttons with one `+` attachment entry that opens a two-action tray only when needed, leaving the main row as `attachments / context / input / send`
  - made the mobile composer close the attachment tray and context controls against each other so the bottom area never expands into stacked utility clutter
  - collapsed low-frequency mobile right-rail controls into a single summary toggle (`工作台设置 · 中文 · gpt-5.4`), with language/model/logout only rendering after explicit expansion
  - tightened mobile search/library density by reusing the compact rail row style for search results, linked files, recent memory, and knowledge rows inside the mobile right sheet
- Production evidence:
  - deployed to `https://chat.aibrowseratlas.com`
  - host-side smoke succeeded after deploy and returned `1.0.1 / v1 / stable`
  - live mobile browser verification confirmed:
    - the composer now shows `打开附件入口` instead of separate file/folder buttons
    - tapping `+` reveals `加文件 / 加文件夹` in a temporary tray
    - the right rail now lands with a collapsed `工作台设置` summary button and only reveals language/model/logout after expansion
  - formal acceptance passed again and wrote:
    - `projects/chat-mirror/data/acceptance/20260316T083018Z.json`
    - `projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - on mobile, the fastest way to improve usability is usually not new gestures; it is moving low-frequency controls off the always-visible row so text entry keeps the width advantage

## AibrowserAtlas added presentation workflow contracts and collapsed the context ledger by default

- Source files changed:
  - `projects/chat-mirror/web/src/App.tsx`
  - `projects/chat-mirror/web/src/styles.css`
  - `projects/chat-mirror/server/src/index.ts`
  - `projects/chat-mirror/scripts/acceptance.sh`
- Concrete action taken:
  - added `presentation` to the same manifest-driven preflight contract used by the newer creation workflows, with a dedicated setup card and a default goal inherited from the current session title when the user does not manually override it
  - tightened `/api/sessions/:id/presentation` so it enforces the required `goal` context, injects that goal into the generation briefing, and persists workflow metadata plus the resolved goal into the saved asset summary
  - reduced right-rail density by making the context ledger card default-collapsed and adding an explicit `展开明细 / 收起明细` toggle instead of always rendering the full metric grid
  - extended formal acceptance so PPT generation is now a release gate: it creates a real deck, downloads the `.pptx`, verifies ZIP headers plus `ppt/presentation.xml` and `ppt/slides/slide1.xml`, and checks that the requested goal survives into the asset summary
- Production evidence:
  - deployed the updated build to `https://chat.aibrowseratlas.com`
  - `/api/version` continued to report `1.0.1 / v1 / stable`
  - formal acceptance passed and wrote `projects/chat-mirror/data/acceptance/20260316T065932Z.json`, with `latest.json` updated to the same run
  - live browser checks confirmed the context ledger now lands collapsed with `展开明细`, and `生成 PPTX` no longer blocks incorrectly when the current session already provides the deck goal
- Guardrail learned:
  - the newer capability surface needs to stay contract-backed end to end; if a workflow is shown as a real product action, acceptance must validate its actual downloadable artifact instead of only checking that an API endpoint returns `200`
    - `goal-manager-auto`, `reviewer-auto`, `evolution-auto`: `0m` because the autonomy loop, not heartbeat, should drive them
  - left the historical cron job `loop-progress-feedback` disabled to avoid duplicate progress spam
- Verified result:
  - `openclaw status --json` now reports the live heartbeat scheduler with the restored cadence above
  - `bash scripts/openclaw-readiness.sh` returns `overall=ok`
  - `python3 scripts/openclaw-autonomy-guard.sh 5` returns `overall=ok`
  - `python3 scripts/openclaw-capability-audit.sh` returns:
    - `runtime_health.status=ok`
    - `acp_orchestration.status=ready`
    - `im_channels.status=ready`
- Operating rule:
  - use heartbeat to keep the main and specialist surfaces warm
  - use host-side ACPX sessions for real multi-step execution
  - use the autonomy loop, not fast heartbeat polling, to keep the auto agents productive

## AibrowserAtlas knowledge-work-plugins direction is now mapped to a concrete productization plan

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - reference: Anthropic `knowledge-work-plugins`
- Diagnosis:
  - `chat-mirror` already has the substrate: sessions, memory scopes, knowledge linking, background imports, unified search, agent routing, and real artifact generation
  - the main missing layer is productization, not raw capability count
  - the critical gaps are:
    - no first-class domain pack manifest layer
    - no search orchestration with query typing, evidence ranking, dedupe, and confidence
    - no tiered memory product model
    - no workflow contracts turning modes into structured jobs
    - no connector registry / graceful degradation model
- Action taken:
  - wrote `/Users/lu/.openclaw/workspace/projects/chat-mirror/docs/KNOWLEDGE_WORK_PLUGINS_IMPLEMENTATION_PLAN.md`
  - set the recommended build order to:
    - pack manifest foundation
    - enterprise-search style retrieval orchestrator
    - structured `write-spec` workflow
    - structured `build-dashboard` workflow
- Guardrail learned:
  - do not keep adding capability tiles before the middle layer exists; the right move is to strengthen retrieval, memory, and workflow contracts first so each visible capability resolves to a real backend path.

## AibrowserAtlas has started the first real knowledge-work-plugins implementation slice

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - first execution slice: `pack manifest + workflow registry + search orchestration + minimal UI adoption`
- Code shipped locally:
  - added `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/packs.ts`
    - first-class pack catalog
    - workflow catalog
    - capability manifest now derived from enabled workflows instead of a hard-coded mode list
  - added `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/search-orchestrator.ts`
    - query classification
    - retrieval policy by query type
    - answer-first workspace search synthesis with confidence, highlights, and source coverage
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/index.ts`
    - new public endpoints: `/api/packs`, `/api/workflows`
    - `/api/search` now returns orchestrated `analysis` in addition to raw result buckets
    - chat context assembly now uses query-type-aware retrieval policy
    - reply trace now includes `retrievalProfile` and `contextLedger.queryType`
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/src/App.tsx`
    - fetches pack/workflow catalog
    - capability cards now inherit workflow metadata
    - search panel now shows synthesized answer, confidence, highlights, and source coverage
    - right rail now shows workflow contract details and current retrieval intent
- Verification:
  - `npm run build` passed in `projects/chat-mirror/server`
  - `npm run build` passed in `projects/chat-mirror/web`
- Guardrail learned:
  - the right first upgrade is not “more buttons”; it is turning search and capability surfacing into typed, inspectable product contracts while preserving the current chat-first interaction.

## AibrowserAtlas write-spec and build-dashboard flows now have real preflight structure

- Scope:
  - second execution slice in `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - focus: `product-management.write-spec` and `data-workbench.build-dashboard`
- Code shipped locally:
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/src/App.tsx`
    - document workflow now captures:
      - `problemStatement`
      - `audience`
      - `successMetric`
      - `constraints`
      - `mustInclude`
    - visualization workflow now blocks generation until:
      - `decision`
      - `primaryMetric`
      - `audience`
      are present
    - right rail now warns when these workflow-critical fields are missing instead of pretending the flow is ready
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/index.ts`
    - document request schema now accepts spec-oriented inputs
    - document and visualization asset summaries now persist `workflowId`, `workflowName`, `packId`, and workflow-specific fields
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/document.ts`
    - prompt and fallback path now bias toward PRD/spec structure when the briefing looks like a spec workflow
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/visualization.ts`
    - prompt and fallback path now bias toward operator-dashboard structure when decision and metric framing are present
- Verification:
  - `npm run build` passed in `projects/chat-mirror/server`
  - `npm run build` passed in `projects/chat-mirror/web`
- Guardrail learned:
  - a workflow becomes real when generation is willing to say “not enough context yet”; silent low-context generation is prompt theater, not product quality.

## AibrowserAtlas research workflow and frontend chunking were tightened in the next slice

- Scope:
  - third execution slice in `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - focus: `product-management.synthesize-research` preflight + frontend build chunking
- Code shipped locally:
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/src/App.tsx`
    - research generation now blocks until:
      - `objective`
      - `scope`
      - `comparisonLens`
      are present
    - workflow-missing hints now cover research-specific fields too
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/index.ts`
    - research asset summaries now persist workflow metadata plus research-specific context fields
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/research.ts`
    - prompt and fallback path now bias toward scoped research workflow instead of generic overview when objective/scope/comparison framing exists
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/vite.config.ts`
    - introduced deterministic vendor chunk splitting for:
      - `react/react-dom`
      - `@radix-ui/*`
      - `lucide-react`
      - remaining misc vendor code
- Verification:
  - `npm run build` passed in `projects/chat-mirror/server`
  - `npm run build` passed in `projects/chat-mirror/web`
  - frontend bundle shape improved from one ~`500 kB` main JS chunk to:
    - `index` ~`236 kB`
    - `vendor-react` ~`190 kB`
    - smaller radix/icon/misc chunks
- Guardrail learned:
  - once workflow preflight is in place, the next reliability win is not more UI but keeping the client chunk graph sane so the product can keep evolving without one oversized entry bundle.

## AibrowserAtlas finance and contract workflows now enforce real preflight and carry workflow identity

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - follow-up slice after pack/workflow/search productization
- Code shipped locally:
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/src/App.tsx`
    - `contract` generation now blocks until critical context exists: `agreement type` and `review goal`
    - `finance` generation now blocks until critical context exists: `subject`, `horizon`, and `risk appetite`
    - right-rail workflow warning now covers `contract` and `finance`, not just `document`, `research`, and `visualization`
    - recent asset rows now show workflow / pack badges when asset summaries include them
    - workspace asset summary types were expanded so workflow metadata and domain-specific fields are typed instead of being opaque JSON
  - updated `/Users/lu/.openclaw/workspace/projects/chat-mirror/server/src/index.ts`
    - `/api/sessions/:id/contract-review` now rejects generation when required workflow fields are missing
    - `/api/sessions/:id/finance` now rejects generation when required workflow fields are missing
    - contract asset summaries now persist `workflowId`, `workflowName`, `packId`, `packName`, and review-specific inputs
    - finance asset summaries now persist `workflowId`, `workflowName`, `packId`, `packName`, and finance-specific inputs
- Verification:
  - `npm run build` passed in `projects/chat-mirror/server`
  - `npm run build` passed in `projects/chat-mirror/web`
- Guardrail learned:
  - workflow contracts are not real until both the UI and the API refuse low-context generation. Frontend-only preflight is still bypassable prompt theater.
- Follow-up hardening in the same slice:
  - server-side required-context enforcement was also extended to `document`, `research`, and `visualization`
  - result: every currently productized structured workflow now has both UI-side and API-side preflight instead of frontend-only checks
- Additional follow-up:
  - workflow required-context keys were normalized into a single-source manifest in `server/src/packs.ts`
  - frontend preflight now derives missing fields from workflow manifest keys instead of hard-coded per-feature arrays
  - backend route enforcement now also resolves required fields from workflow manifest ids instead of duplicating lists per route
  - local runtime validation passed after restoring the cached `better-sqlite3` prebuilt for Node 22 via `prebuild-install`
  - verified `GET /api/workflows` returns stable keys such as `target_user`, `comparison_lens`, `agreement_type`, and `risk_appetite`

## AibrowserAtlas workflow-contract productization is now deployed, accepted, and pushed

- Project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
- Runtime code commit deployed to production:
  - `077530c` `Productize workflow contracts and retrieval`
- Follow-up repo commit for release-gate alignment:
  - `7957852` `Align acceptance with workflow preflight`
- Deployment result:
  - production deploy to the existing host succeeded through `scripts/deploy.sh`
  - remote Docker build completed successfully and smoke returned:
    - `{"ok":true,"upstream":"https://ttqq.inping.com/v1","version":"1.0.1","releaseStage":"v1","releaseChannel":"stable"}`
- Public verification:
  - `https://chat.aibrowseratlas.com/api/workflows` now returns stable required-context keys like `agreement_type`, `review_goal`, `subject`, `horizon`, and `risk_appetite`
  - homepage now serves the freshly built frontend asset `assets/index-DdPhuX6m.js`
- Acceptance:
  - initial acceptance failed honestly because `scripts/acceptance.sh` still used the pre-productization document payload and did not supply `problemStatement` / `successMetric`
  - patched the acceptance script to match the new workflow contract
  - reran acceptance successfully; full public release checks passed and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T043209Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- GitHub:
  - pushed `main` to `https://github.com/miounet11/myjizuo.git`
- Guardrail learned:
  - once workflow generation becomes explicitly contract-driven, release gates must evolve in the same turn; otherwise acceptance starts failing for the right reason and becomes stale rather than trustworthy.

## AibrowserAtlas expanded manifest-driven preflight across website and secondary artifact workflows

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - follow-up slice after the initial workflow-contract rollout
- Code shipped locally and deployed:
  - frontend `web/src/App.tsx`
    - expanded `STRUCTURED_PREFLIGHT_KINDS` so UI-side blocking now also covers:
      - `website`
      - `story`
      - `spreadsheet`
      - `playbook`
      - `design`
      - `schedule`
    - added first-class website setup fields for `goal` and `audience`
    - website generation now routes through the same manifest-driven missing-field check as the other structured workflows
    - right-rail setup copy for website now reflects real launch planning instead of template-only selection
  - backend `server/src/index.ts`
    - website request schema now accepts `goal` and `audience`
    - API-side required-context enforcement now also blocks low-context generation for:
      - `creation-studio.build-website`
      - `creation-studio.story-kit`
      - `data-workbench.build-spreadsheet`
      - `product-management.build-playbook`
      - `creation-studio.design-brief`
      - `data-workbench.schedule-automation`
    - asset summaries for these artifact routes now persist workflow / pack identity and key setup fields
  - acceptance `scripts/acceptance.sh`
    - website generation payload now includes required `goal` and `audience`
    - acceptance now verifies that website asset summary preserved those inputs
  - desktop UI polish `web/src/styles.css`
    - softened the left-rail top `新建` button so it reads as a secondary control rather than a dominant CTA
- Deployment and public validation:
  - production redeploy succeeded on `96.44.158.158`
  - smoke returned:
    - `{"ok":true,"upstream":"https://ttqq.inping.com/v1","version":"1.0.1","releaseStage":"v1","releaseChannel":"stable"}`
  - fresh public acceptance passed and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T060355Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- Browser-level live evidence:
  - on `https://chat.aibrowseratlas.com/`, after creating a real session and switching to `做网页`, the right rail warned:
    - `先补齐这些关键信息再生成：目标、面向对象。`
  - expanding `按需细调` exposed the new website setup fields:
    - `核心受众`
    - `网站目标`
  - this confirms the UI now blocks low-context website generation before the request hits the API
- Guardrail learned:
  - a workflow contract is only fully productized when manifest keys, frontend preflight, backend enforcement, acceptance payloads, and live browser behavior all agree on the same required context.

## AibrowserAtlas mobile search compression and message-density pass is now deployed and accepted

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - follow-up slice after the mobile attachment / rail simplification work
- Code shipped:
  - `web/src/App.tsx`
    - added mobile-only search section truncation at 3 rows per section
    - added per-section expand / collapse controls for `sessions`, `knowledge`, and `memories`
    - reset mobile section expansion state when query, session, or viewport changes so stale expansion does not leak across searches
  - `web/src/styles.css`
    - tightened mobile assistant reading density by reducing bubble spacing, support-rail spacing, context-ledger padding, and rich-text rhythm
    - added search-section header / count / toggle styles so compressed result groups stay legible without adding new conceptual load
- Deployment and release evidence:
  - production redeploy to `96.44.158.158` succeeded through `scripts/deploy.sh`
  - remote smoke returned:
    - `{"ok":true,"upstream":"https://ttqq.inping.com/v1","version":"1.0.1","releaseStage":"v1","releaseChannel":"stable"}`
  - fresh public acceptance passed and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T092627Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- Live verification:
  - mobile Playwright viewport `390x844` confirmed the right rail settles into visible mobile-open state when opened, with the rail landing at roughly `x=31px`
  - seeded 5 workspace memories into a clean temporary acceptance account and confirmed `/api/search?q=atlas-mobile-1773652947907` returned all 5 memory hits on production, covering the compressed-result path with real data
- Guardrail learned:
  - mobile result compression should reduce list length without inventing new modes; section-local expand toggles are the right ceiling before the UI starts teaching more than it helps.

## AibrowserAtlas desktop empty-state launch pad and first-turn bridge are now productized

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - focus: desktop `new chat` and early conversation state
- Code shipped:
  - `web/src/App.tsx`
    - added localized desktop launch-pad labels and lightweight workspace-stage signal helpers
    - added a 4-card desktop summary band for routing, memory, outputs, and recovery in the empty state
    - upgraded the right-side empty-state studio block with a clearer “what compounds” header and persistent asset note
    - upgraded the early-conversation bridge card to include lightweight signals for messages, files, and context instead of a plain text-only strip
  - `web/src/styles.css`
    - added unified card styling for empty-state summary cards and first-turn bridge signals
    - tightened section hierarchy for the desktop launch pad and made the new components responsive down through tablet and mobile breakpoints
- Deployment and release evidence:
  - production redeploy to `96.44.158.158` succeeded through `scripts/deploy.sh`
  - remote smoke returned:
    - `{"ok":true,"upstream":"https://ttqq.inping.com/v1","version":"1.0.1","releaseStage":"v1","releaseChannel":"stable"}`
  - fresh public acceptance passed and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T095306Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- Browser evidence:
  - live desktop empty state on `https://chat.aibrowseratlas.com/` now shows a real launch-pad band with:
    - `Workspace launch pad`
    - routing / memory / outputs / recovery cards
  - after sending a first message, the bridge card shows:
    - `What compounds in this thread`
    - lightweight metrics for `Messages`, `Files`, and `Context`
- Guardrail learned:
  - desktop empty states should explain the product’s operating model in one screen, but the explanation has to stay subordinate to the actual next action; metrics and recovery cues are useful, new workflow concepts are not.

## OpenClaw Telegram no-reply path now retries transient send failures in the live gateway bundle

- Root cause verified from live evidence:
  - `@atlas_oc_architect_20260313_bot` DM traffic reached the session, but the lane hit a long LLM timeout and the eventual Telegram reply then failed on transient network send:
    - `2026-03-16T17:21:36+08:00` `lane=session:agent:architect:main ... FailoverError: LLM request timed out.`
    - `2026-03-16T17:21:54+08:00` `telegram sendMessage failed: Network request for 'sendMessage' failed!`
    - `2026-03-16T17:21:55+08:00` `telegram message processing failed: HttpError: Network request for 'sendMessage' failed!`
  - Telegram replies sent from the Telegram plugin path were using direct `bot.api.sendMessage(...)` flows, not the durable outbound delivery queue, so transient network failures could drop the reply immediately.
- Action taken:
  - copied the live bundle `/opt/homebrew/lib/node_modules/openclaw/dist/reply-Bm8VrLQh.js` into workspace staging under `.tmp/openclaw-dist/`
  - patched `withTelegramApiErrorLogging(...)` to retry transient Telegram/network API failures 3 times with bounded backoff before surfacing a hard failure
  - patched the outer Telegram processing fallback so its emergency `sendMessage` path also goes through `withTelegramApiErrorLogging(...)` instead of a bare one-shot send
  - installed the patched bundle back into the live OpenClaw dist and restarted the gateway
- Verification:
  - staged bundle passed `node --check`
  - live bundle contains:
    - `telegram ${operation} transient failure; retry ...`
    - `sendMessage (processing error fallback)`
  - `openclaw gateway status` returned `RPC probe: ok` after restart
  - gateway restart log shows fresh provider startup at `2026-03-16T17:36:14+08:00` for `architect`, `coder`, `main`, `reviewer`, `tester`
- Rollback path:
  - original pre-patch bundle preserved at:
    - `/Users/lu/.openclaw/workspace/.tmp/openclaw-dist/reply-Bm8VrLQh.js.orig`
- Guardrail learned:
  - when Telegram reply delivery lives outside the durable outbound queue, transient network faults become silent user-visible drops; every final reply path needs either queue-backed recovery or explicit transient retry.

## AibrowserAtlas chat upstream failure handling hardened on production

- User-reported symptom:
  - generating a PPT through the workspace surfaced a generic `Upstream error`
- What I verified:
  - public acceptance before the fix still passed end-to-end, including PPT generation, so the issue was intermittent rather than a total PPT route outage
  - the stronger failure point was the live chat streaming path, which previously surfaced upstream failures directly to the UI without a recovery path
- Action taken in `/Users/lu/.openclaw/workspace/projects/chat-mirror`:
  - added upstream response parsing and sanitized failure summarization in `server/src/index.ts`
  - changed `/v1/chat/completions` to log upstream status/details and return a productized failure message instead of raw `Upstream error`
  - changed `/api/chat/stream` to fall back from streaming mode to buffered non-stream completion when the upstream stream returns non-OK, lacks a readable body, or throws before completion
  - changed `web/src/App.tsx` so API failures prefer `detail` over the generic `error` field, preventing the UI from collapsing to a useless `Upstream error` label when better detail exists
- Deployment and verification:
  - deployed to `96.44.158.158:/srv/chat-mirror`
  - post-deploy smoke on the host returned `{"ok":true,"upstream":"https://ttqq.inping.com/v1","version":"1.0.1","releaseStage":"v1","releaseChannel":"stable"}`
  - post-deploy live stream smoke against `https://chat.aibrowseratlas.com/api/chat/stream` returned the expected SSE sequence: `ready -> delta -> done`, with no `event:error`
  - post-deploy public acceptance passed again and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T103200Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - intermittent upstream instability should not be allowed to surface as a dead-end product error when a non-stream completion path can still recover the turn and preserve the session.

## AibrowserAtlas stream-stall UX and fallback recursion fixed on production

- Fresh user-facing symptom:
  - sending a chat turn sometimes showed no visible motion, even though the request had been sent
- Root cause verified from live logs after the prior upstream hardening deploy:
  - a real request hit `/api/chat/stream` on `2026-03-16` and the upstream returned `504`
  - server log showed one fallback attempt from `upstream-http-504`, then a second fallback attempt from `stream-exception`, meaning the fallback failure path re-entered the same fallback logic and extended the dead-air wait
  - the frontend also had no explicit product status for “streaming path is congested, switching to stable mode”, so the assistant bubble could look frozen until text finally arrived or failed
- Action taken in `/Users/lu/.openclaw/workspace/projects/chat-mirror`:
  - changed `server/src/index.ts` so chat stream fallback is attempted only once per failure branch
  - added an explicit SSE `status` event with phase `buffered-fallback` before switching from streaming to buffered completion
  - added a centralized `failStreamingTurn(...)` path so fallback failure ends immediately with a productized error instead of recursively re-entering fallback
  - changed `web/src/App.tsx` to handle the new `status` event and surface a visible stabilizing message in both the workspace status line and the placeholder assistant message
  - added localized `stabilizing` status copy for `en`, `zh`, and `ja`
- Deployment and verification:
  - deployed to `96.44.158.158:/srv/chat-mirror`
  - direct live stream smoke after deploy returned normal `ready -> delta -> done` on `https://chat.aibrowseratlas.com/api/chat/stream`
  - post-deploy service log showed the smoke request completed cleanly with HTTP 200 in about 5.6s
  - full acceptance passed again and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T114733Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - if a stream-recovery path can fail, it must never recurse back into itself; fallback must be single-shot, immediately visible to the user, and terminate cleanly on the first unrecoverable failure.

## AibrowserAtlas model selection now respects the frontend instead of agent defaults

- User concern verified:
  - production chat still hit `504`, and the user explicitly questioned whether requests were being sent to the wrong model
  - the expected product rule was: the frontend model selector should be usable, and the default should remain `gpt-5.4`
- What I confirmed with live evidence:
  - production `DEFAULT_MODEL` is `gpt-5.4`
  - frontend initial model is `gpt-5.4`
  - live built-in agents are:
    - `iAtlas Mission Control` -> `gpt-5.4`
    - `iAtlas Knowledge Curator` -> `gpt-5.4`
    - `iAtlas Delivery Operator` -> `gpt-5.4`
    - `iAtlas Web App Analyst` -> `grok-420-agents`
  - the real bug was not the env default, but precedence and UI behavior:
    - backend prioritized `Agent.default_model` over `body.model`
    - frontend model dropdown showed `currentAgent.defaultModel || model`
    - frontend disabled model selection whenever a current agent existed
- Action taken in `/Users/lu/.openclaw/workspace/projects/chat-mirror`:
  - changed backend model resolution to `body.model || Agent.default_model || DEFAULT_MODEL`
  - added `modelUsed` into server trace and included `model` in the stream `ready` event payload
  - changed frontend selected-model source to the real local `model` state instead of `currentAgent.defaultModel`
  - removed the dropdown disable-on-agent behavior so the user can keep choosing models in bound sessions
  - prevented agent changes from overwriting the model after the user has manually selected one
- Verification:
  - deployed to `96.44.158.158:/srv/chat-mirror`
  - direct live stream smoke with explicit payload `"model":"gpt-5.4"` returned:
    - `event: ready ... "model":"gpt-5.4"`
    - `trace.modelUsed: "gpt-5.4"`
  - full public acceptance passed again and wrote:
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/20260316T131516Z.json`
    - `/Users/lu/.openclaw/workspace/projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - if the product surfaces a model selector, the chosen model must be authoritative; showing a selector while letting agent defaults silently override it creates a false-control UX and makes outage diagnosis much harder.

## AibrowserAtlas 1.0.1 client packaging was rebuilt and the live download center was refreshed

- Scope:
  - project: `/Users/lu/.openclaw/workspace/projects/chat-mirror`
  - release line: `AibrowserAtlas v1.0.1`
  - live download API: `https://chat.aibrowseratlas.com/api/downloads`
- Concrete work completed:
  - rebuilt Android `release APK`, `debug APK`, and `release AAB` using the provided keystore at `/Users/lu/Downloads/signature_all/android/mlchat.jks`
  - repaired macOS Developer ID signing by importing the provided certificate into a temporary keychain and adding the missing Apple `Developer ID Certification Authority G2` intermediate from `certs.apple.com/devidg2.der`
  - confirmed the Apple certificate had only become valid starting `2026-03-16 09:01:46 UTC`; earlier signing attempts before that validity window would fail or look invalid
  - removed `mac.identity: null` so `electron-builder` can use a real Developer ID identity when present
  - removed `dmg.sign: false` and manually signed the generated `DMG`, leaving the mac deliverable in the truthful state `Developer ID signed, not notarized`
  - moved `@capacitor/*` packages out of runtime `dependencies` and into `devDependencies` so desktop builds no longer package mobile toolchains or Android build intermediates
  - regenerated `release-downloads/catalog.json` and refreshed `release-downloads/files/*`
  - synced `release-downloads/` to production and rebuilt the container so the baked-in download catalog matched the new artifacts
- Verified result:
  - live `api/downloads` now reports `generatedAt=2026-03-16T14:40:05.682Z`
  - live download headers now match the rebuilt artifacts:
    - mac DMG `111538250` bytes
    - Windows setup `95656831` bytes
    - Android release APK `9186577` bytes
  - remote smoke returned `{"ok":true,"upstream":"https://ttqq.inping.com/v1","version":"1.0.1","releaseStage":"v1"}`
- Guardrails learned:
  - if `release-downloads` is copied into the runtime image in `Dockerfile`, syncing files on the server is not enough; the container must be rebuilt for `/api/downloads` and `/downloads/files/*` to update
  - current mac signing is materially better than ad-hoc signing, but Gatekeeper still reports `Unnotarized Developer ID` until Apple notarization is configured

## AibrowserAtlas public release CTA was moved off private GitHub and onto a first-party release page

- Problem found:
  - the marketing download section linked `Official release` to `https://github.com/miounet11/myjizuo/releases/tag/v1.0.1`
  - the repo is private, so that URL is not a valid public release surface for normal users
- Action taken:
  - added `/Users/lu/.openclaw/workspace/projects/chat-mirror/web/public/release.html` as a first-party public release page
  - changed `web/src/App.tsx` so the release CTA now points to `/release.html` instead of a private GitHub releases URL
  - deployed the updated frontend and verified `https://chat.aibrowseratlas.com/release.html` is live
- Guardrail learned:
  - if the product repo is private, never use GitHub release URLs as a public website CTA; first-party release truth must stay on first-party domains

## Apple release readiness is now scripted instead of narrative-only

- Action taken:
  - added `projects/chat-mirror/scripts/apple-release-doctor.sh`
  - added `projects/chat-mirror/scripts/notarize-mac.sh`
  - added `projects/chat-mirror/docs/APPLE_RELEASE_READINESS.md`
  - wired `scripts/package-shell.sh doctor-apple` and `scripts/package-shell.sh notarize-mac`
- Verified result on this host:
  - `doctor-apple` now reports the real remaining blockers instead of leaving Apple release state implicit
  - mac notarization is narrowed to one missing input: `APPLE_NOTARY_ISSUER`
  - iOS export is still blocked by two concrete facts: full Xcode unavailable and provisioning profile bundle id mismatch (`com.qs.mlchat` vs `com.aibrowseratlas.mobile`)
- Guardrail learned:
  - for Apple release work, a good control plane is worth more than more guessing; once the exact missing values are scripted and reproducible, release work stops thrashing.
