## 2026-03-19 — mydoc A+B upgrade completed: first-party publisher + public/private split

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/docs/AGENT_LEARNING_PROTOCOL.md`
  - `/Users/lu/.openclaw/LEARNING_REDESIGN.md`
  - `/Users/lu/.openclaw/docs/agi-implementation.md`
  - `/Users/lu/.openclaw/docs/agi-roadmap.md`
  - `/Users/lu/.openclaw/memory-tree/core/MEMORY_TREE.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc.sh`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc-private.sh`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc-public.sh`
  - remote `/etc/caddy/Caddyfile.d.mydoc`
- Hypothesis:
  - The right durable repair is twofold: codify publication as a first-party script and split the doc surface into public/private paths so access control no longer depends on all-or-nothing site auth.
- Action taken:
  - extended `publish-mydoc.sh` with `--visibility private|public`
  - created wrappers `publish-mydoc-private.sh` and `publish-mydoc-public.sh`
  - introduced content roots `my-doc-site/content/private/` and `my-doc-site/content/public/`
  - changed remote Caddy config so only `/content/private/*` requires Basic Auth while the site root and other content paths remain public
  - published the imiaoda document into the private tree
- Result / evidence:
  - private doc URL: `https://mydoc.inping.com/content/private/docs/imiaoda-content-growth-plan.md`
  - unauthenticated private access returns `401`
  - authenticated private access returns `200`
  - root site `https://mydoc.inping.com/` returns `200`
- OpenClaw capability improved:
  - mydoc now has both a repeatable publisher and a path-level security model instead of a fragile all-site lock
- Guardrail learned:
  - shareable docs should default to explicit visibility selection at publish time, not implicit assumptions after publication
  - auth should protect private content, not unnecessarily break the public homepage/index
- Yangming check:
  - yes: the user preference for A+B became executable scripts, server config changes, and verified access behavior
- Next improvement:
  - move Basic Auth secrets out of ad hoc command env, add a small publish manifest or metadata convention for default visibility, and hide private items from the public index page

## AibrowserAtlas generator-method upgrade shipped: UI method docs plus stronger website/design/playbook/automation outputs

- Action taken:
  - audited the live artifact stack in `projects/chat-mirror/server/src/*` and confirmed the real leverage point is the generator contract layer: workflow manifest -> request briefing -> prompt schema -> parser -> bundle writer
  - added two internal execution docs:
    - `projects/chat-mirror/docs/STITCH_METHOD_ABSORPTION.md`
    - `projects/chat-mirror/docs/GOAL_DRIVEN_UI_WORKFLOW.md`
  - upgraded the server-side generation method so outputs now carry stronger system structure instead of only content:
    - `design.ts`: added goal, primary user action, key states, component map, variants, and prototype notes plus new bundle files such as `KEY_STATES.md`, `COMPONENT_MAP.md`, and `PROTOTYPE_PLAN.md`
    - `website.ts`: added goal, primary audience action, information architecture, user flows, component guide, and implementation sequence plus new bundle files such as `INFORMATION_ARCHITECTURE.md`, `COMPONENT_GUIDE.md`, and `IMPLEMENTATION_PLAN.md`
    - `playbook.ts`: added decision checkpoints, required artifacts, and failure modes plus new bundle files `DECISION_CHECKPOINTS.md` and `ARTIFACTS.md`
    - `schedule.ts`: added explicit `OPERATOR_CHECKPOINTS.md` output to align automation packages with the human-in-the-loop operating model
  - updated `packs.ts` output contracts and summaries so the product's own workflow catalog matches the richer generator outputs
  - updated `server/src/index.ts` request briefings so the model is explicitly steered toward goal -> structure -> state -> component -> implementation thinking instead of prompt-only drafting
  - deployed the changed server files to `96.44.158.158:/srv/chat-mirror` and rebuilt the production container successfully
- Verified result:
  - local `server` build passed
  - local `web` build passed
  - local runtime probe successfully executed the built bundle writers and produced real ZIP buffers:
    - design: 3 screens, 4 states, 5 components
    - website: 4 pages, 2 flows, 3 components, 22 files
    - playbook: 4 steps, 2 checkpoints, 3 artifacts
    - automation: 2 jobs, 3 delivery routes
  - production rebuilt cleanly and both runtime health probes stayed green:
    - remote `http://127.0.0.1:8791/api/version`
    - public `https://chat.aibrowseratlas.com/api/version`
- Guardrail learned:
  - if we want artifact quality to improve materially, the right place to encode the method is the generator contract layer, not only homepage copy or starter prompts; otherwise the UI sounds smarter while the exported deliverables stay shallow.

## AibrowserAtlas deck/document contract upgrade shipped: presentation and document generators now preserve structure, decisions, and next-step logic

- Action taken:
  - upgraded `projects/chat-mirror/server/src/presentation.ts` from a shallow slide list into a richer deck contract with:
    - audience action
    - narrative arc
    - section plan
    - proof points
    - slide roles, key messages, and evidence hints
  - upgraded `projects/chat-mirror/server/src/document.ts` from a title/summary/sections shape into a stronger document contract with:
    - goal
    - audience
    - intended use
    - tone
    - structure logic
    - section purpose plus key points
    - decision points
    - next steps
  - updated `projects/chat-mirror/server/src/packs.ts` so the public workflow catalog truthfully describes the stronger deck/doc outputs
  - updated `projects/chat-mirror/server/src/index.ts` so request briefings, parser calls, and asset summaries carry the richer structure through the live API
  - extended asset email summaries so goal, audience, narrative/proof counts, and decision/next-step counts are visible when present
  - deployed the upgraded server to `96.44.158.158:/srv/chat-mirror` and rebuilt production successfully
- Verified result:
  - local `server` build passed
  - direct local runtime probe produced real artifacts from the upgraded contracts:
    - presentation: 6 slides, 3 sections, 3 proof points, 3 narrative beats
    - document: 5 sections, 2 decision points, 3 next steps, 4 structure-logic bullets
  - production health probes stayed green:
    - remote `http://127.0.0.1:8791/api/version`
    - public `https://chat.aibrowseratlas.com/api/version`
  - full live acceptance passed and refreshed:
    - `projects/chat-mirror/data/acceptance/20260319T071140Z.json`
    - `projects/chat-mirror/data/acceptance/latest.json`
  - live acceptance specifically confirmed:
    - `presentation_generation`: ok with valid downloadable PPTX
    - `document_generation`: ok with valid downloadable DOCX
- Guardrail learned:
  - presentation/doc upgrades are only real when the parser, fallback spec, artifact writer, workflow catalog, and acceptance evidence all move together; improving prompt text alone would still leave shallow exports in production.

## AibrowserAtlas deck/document setup surface now exposes richer structure inputs, and the live recent-assets rail reflects them

- Action taken:
  - extended `projects/chat-mirror/server/src/index.ts` request schemas so presentation generation now accepts `audience`, `audienceAction`, and `mustProve`, while document generation now accepts `intendedUse`
  - updated the presentation/document briefing builders and endpoint override logic so these user-entered setup fields survive both primary generation and fallback generation
  - updated `projects/chat-mirror/server/src/document.ts` fallback behavior so a provided `Intended use` value is preserved instead of being replaced by a generic default sentence
  - expanded `projects/chat-mirror/web/src/App.tsx` capability setup UI for deck/doc generation and added richer localized field labels in English, Chinese, and Japanese
  - changed presentation asset requests to send the full deck setup payload, not only the goal
  - added compact structure previews to the recent-assets list so deck/document rows now surface goal, audience, intended action/use, and key structure metrics directly inside the existing right-rail rows
  - redeployed the changed files to `96.44.158.158:/srv/chat-mirror` and refreshed live acceptance
- Verified result:
  - local `server` build passed
  - local `web` build passed
  - live frontend bundle served the new structure-field strings, including:
    - `What the audience should do next`
    - `What this deck must prove`
    - `How this document will be used`
  - full live acceptance passed and refreshed:
    - `projects/chat-mirror/data/acceptance/20260319T074144Z.json`
    - `projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - if the UI exposes higher-leverage setup fields, the API schema, request briefing, fallback path, asset summary, and recent-asset preview all need to move together; otherwise the product suggests structure it does not actually preserve.

## AibrowserAtlas website/design setup now follows the same goal-to-action method as deck/doc generation, and the live asset drawer exposes the stronger structure

- Action taken:
  - extended `projects/chat-mirror/server/src/index.ts` so website generation now accepts `primaryAudienceAction`, and design generation now accepts `goal` plus `primaryUserAction`
  - updated the website/design request briefing builders so the server explicitly steers generation by goal, next action, structure, and reuse instead of only a shallow brief
  - updated the website and design generation endpoints so these new setup fields are preserved in the generated spec and written into asset summaries
  - expanded `projects/chat-mirror/web/src/App.tsx` so website setup now asks what the visitor should do next, and design setup now captures the design goal plus the next user action
  - upgraded the recent-assets drawer so website/design rows now show structure previews instead of only counts: goal, audience, next action, and key structure metrics
  - updated asset email summaries to include action, component, state, and variant counts where available
  - redeployed production to `96.44.158.158:/srv/chat-mirror`
- Verified result:
  - local `server` build passed
  - local `web` build passed
  - remote smoke on `http://127.0.0.1:8791/api/version` stayed green after deploy
  - live public bundle served the new website/design UI strings, including:
    - `What the visitor should do next`
    - `What the user should do next`
  - full live acceptance passed and refreshed:
    - `projects/chat-mirror/data/acceptance/20260319T080657Z.json`
    - `projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - when one capability family graduates from prompt-like setup to real product structure, adjacent creation flows need the same goal-to-action contract or the workspace starts feeling uneven again.

## AibrowserAtlas research/playbook setup now captures the decision or operator outcome up front, and the live asset drawer surfaces those structures instead of only counts

- Action taken:
  - extended `projects/chat-mirror/server/src/research.ts` so research specs now include `decisionToResolve`, and extended `projects/chat-mirror/server/src/playbook.ts` so playbook specs now include `primaryOutcome`
  - updated both generator prompts and fallback specs so the exported artifacts carry a real decision frame or end-state, not only generic scope/purpose language
  - extended `projects/chat-mirror/server/src/index.ts` request schemas and briefing builders so research now accepts `decisionToResolve` and playbook now accepts `primaryOutcome`
  - updated the research/playbook endpoints so these new setup fields are preserved in the generated spec, reflected in workspace asset summaries, and included in email summaries
  - expanded `projects/chat-mirror/web/src/App.tsx` so research setup now asks which decision the research should resolve, and playbook setup now asks what a successful run should produce
  - added structured recent-asset previews for research and playbook rows so the right drawer now shows objective / audience / decision and purpose / audience / outcome instead of only file metrics
  - redeployed production to `96.44.158.158:/srv/chat-mirror`
- Verified result:
  - local `server` build passed
  - local `web` build passed
  - remote smoke on `http://127.0.0.1:8791/api/version` stayed green after deploy
  - live public bundle served the new setup strings:
    - `Decision this research should resolve`
    - `What a successful run should produce`
  - full live acceptance passed and refreshed:
    - `projects/chat-mirror/data/acceptance/20260319T083218Z.json`
    - `projects/chat-mirror/data/acceptance/latest.json`
- Guardrail learned:
  - for decision-support and operating workflows, users need the system to preserve the decision or outcome they are driving toward; otherwise the exported artifact can still be structurally valid while feeling strategically under-specified.
