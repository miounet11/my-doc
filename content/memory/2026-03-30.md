# 2026-03-30

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `scripts/sync-mydoc-workspace.sh`, `my-doc-site/config/workspace-sync-manifest.json`, `my-doc-site/.last-workspace-sync.json`, `my-doc-site/.last-workspace-deploy.json`.
- Hypothesis: a recurring deliverable-honesty defect exists around mydoc publishing because stale sync/deploy state can be mistaken for proof that a newly created document was mirrored and published.
- Evidence found: the source doc `docs/AUTO_GROWTH_ENGINE_DEV_DOC.md` exists, but `my-doc-site/content/docs/AUTO_GROWTH_ENGINE_DEV_DOC.md` was missing; `.last-workspace-sync.json` and `.last-workspace-deploy.json` both remained at 2026-03-25, so they were stale evidence for today's artifact.
- Action taken: wrote `docs/MYDOC_PUBLISH_GUARDRAIL.md` to codify the rule that a mydoc URL must not be presented as delivered unless source file, mirror file, fresh sync evidence, and—if claimed—fresh deploy verification all exist for the current artifact.
- Result/evidence: new doctrine file `docs/MYDOC_PUBLISH_GUARDRAIL.md` exists and turns the observed mydoc publication failure mode into an explicit guardrail and labeling rule (`local-only`, `mirrored-local`, `published-verified`, `unverified-publish`).
- OpenClaw capability improved: stronger deliverable honesty and observability for the mydoc publish path; reduced risk of reusing stale successful deploy state as proof for new artifacts.
- Guardrail learned: old successful deploy metadata is historical evidence only; without a fresh mirror file and fresh sync/deploy state, the correct label is not published but `local-only` or `unverified-publish`.
- Yangming check: yes — the publishing failure was turned into explicit doctrine rather than remaining only as conversational explanation.
- Next improvement: add an executable preflight check that compares source path, mirror path, and sync/deploy freshness before any mydoc URL is returned.
- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, benchmark-runner verification command path.
- Hypothesis: weekly benchmark verification still had a doctrine gap under sandbox restrictions because a blocked command could be noticed in daily memory but not promoted into heartbeat instructions.
- Evidence found: attempted command `test -f ./evals/benchmark-runner.sh && echo EXISTS || echo MISSING
if test -f ./evals/benchmark-runner.sh; then
  printf '\n---HEAD---\n'
  sed -n '1,220p' ./evals/benchmark-runner.sh
fi` failed under tool policy. First error: `exec host not allowed (requested sandbox; configure tools.exec.host=gateway to allow).` Second error after retry: `exec denied: allowlist miss`.
- Action taken: tightened `HEARTBEAT.md` weekly benchmark instructions so blocked verification must record the full command and exact tool error in daily memory and label the review as `blocked`/`degraded` instead of silently skipping it.
- Result/evidence: `HEARTBEAT.md` now contains the blocked-verification handling rule, creating an inspectable doctrine artifact for this exact failure mode.
- OpenClaw capability improved: stronger guardrail quality and observability around blocked execution during weekly benchmark review.
- Guardrail learned: blocked verification is itself a heartbeat defect; it must be labeled honestly and promoted into doctrine when recurring.
- Yangming check: yes — the blocked command became a protocol repair.
- Next improvement: when command execution becomes available, replace blocked inspection with direct file or runner verification and continue labeling any mock/static evidence explicitly.

- Source files reviewed this run: `docs/EVIDENCE_AND_EVAL_RULES.md`, `docs/OPENCLAW_2026_3_8_E2E_ACCEPTANCE.md`, `evals/README.md`, official OpenClaw release/docs pages, official `maoclaw` repo README, `xinxiang.xin` release/docs surface.
- Hypothesis: replacing OpenClaw immediately would be another unmeasured architecture bet; the right move is a same-workload acceptance comparison against `maoclaw`.
- Action taken: created `docs/OPENCLAW_VS_MAOCLAW_ACCEPTANCE_MATRIX_2026-03-30.md` and `evals/runtime-replacement-scorecard-template.json`.
- Result/evidence: the workspace now contains a 7-day replacement harness with cutover thresholds, same-task evaluation categories, and a machine-readable scorecard template.
- OpenClaw capability improved: replacement decisions can now be made through an evidence-based harness instead of frustration or sunk-cost drift.
- Guardrail learned: do not migrate or rewrite based only on architecture aesthetics or current irritation; require same-task verified wins on continuity, context recovery, and operational truthfulness.
- Yangming check: yes — the migration debate was converted into an executable evaluation artifact.
- Next improvement: run day-1 comparison tasks on both systems and fill the first scorecard entries with verified evidence only.

- Source files reviewed this run: `scripts/openclaw-handoff.sh`, `docs/CONTEXT_RECOVERY_PROTOCOL.md`, `scripts/openclaw-session-evidence.sh`, runtime surfaces under `~/.openclaw`, and the newly installed `maoclaw` local runtime.
- Hypothesis: the highest-leverage OpenClaw repair is to make context/history surfaces auditable and to stop placeholder handoffs from being mistaken for real continuity artifacts.
- Action taken: created `scripts/openclaw-context-recovery-audit.sh`, updated `docs/CONTEXT_RECOVERY_PROTOCOL.md` to explicitly include `~/.openclaw/memory` and gateway/session-store authority, and added `audit` plus completeness labeling to `scripts/openclaw-handoff.sh`.
- Result/evidence: `scripts/openclaw-context-recovery-audit.sh` now reports verified workspace memory, verified external `~/.openclaw/memory`, verified `~/.openclaw/sessions/sessions.json`, and flags the latest handoff as `incomplete`; `bash scripts/openclaw-handoff.sh audit 5` warns that the current handoff is not a real continuation package yet.
- OpenClaw capability improved: context recovery is now inspectable, and handoff quality can be downgraded from the tool surface instead of being assumed from file existence alone.
- Guardrail learned: a readable handoff path is not enough; TODO-filled handoffs must be labeled `incomplete` and excluded from high-confidence continuity claims.
- Yangming check: yes — the context/history diagnosis became an executable audit path rather than staying as chat analysis.
- Next improvement: use the new audit output to drive one real handoff rewrite and one retrieval-order repair in runtime/bootstrap logic.

- Source files reviewed this run: `~/.pi/agent/settings.json`, `~/.pi/agent/models.json`, mirrored OpenClaw `openai` provider settings in `/Users/lu/.openclaw/openclaw.json`, and `maoclaw` docs for `models.json` support.
- Hypothesis: `maoclaw` should be compared only after it is wired to the same live OpenAI-compatible route and default model as OpenClaw.
- Action taken: wrote mirrored provider settings into `~/.pi/agent/settings.json` and `~/.pi/agent/models.json` so `maoclaw` uses `provider=openai`, `model=gpt-5.4`, `baseUrl=https://ttqq.inping.com/v1`, `api=openai-completions`, and the same route key as OpenClaw.
- Result/evidence: `pi --list-models gpt-5.4` now shows the mirrored `openai gpt-5.4` entry; `pi -p "Reply with exactly: OK"` returned `OK`; `pi` successfully read `/Users/lu/.openclaw/workspace/memory/2026-03-29.md` and extracted the unfinished job; `pi -c -p "Reply with exactly: CONTINUED"` returned `CONTINUED`.
- OpenClaw capability improved: the replacement comparison is now same-route and same-model instead of being distorted by unrelated `maoclaw` auth defaults.
- Guardrail learned: early pilot blockers must be revisited after environment parity is established; once mirrored config works, the old `maoclaw auth-blocked` state becomes historical, not current truth.
- Yangming check: yes — the comparison moved from setup diagnosis to same-provider execution.
- Next improvement: compare deeper session persistence and multi-step continuity behavior under the mirrored provider path.
- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/MYDOC_PUBLISH_GUARDRAIL.md`.
- Hypothesis: OpenClaw still lacked a durable promotion rule for when repeated corrections should graduate from daily memory into reusable skill/protocol assets, which risks relearning the same workflow lessons conversationally.
- Evidence found: today produced two doctrine-level repairs (`MYDOC_PUBLISH_GUARDRAIL.md` and the blocked-verification update in `HEARTBEAT.md`) and the user explicitly highlighted Claude Skills 2.0 as a model for turning workflow knowledge into reusable assets.
- Action taken: wrote `docs/SKILL_ASSETIZATION_TRIGGER_RULE.md` to define when recurring lessons must be promoted into a checklist, protocol, or `SKILL.md`, plus the minimum structure a promoted skill should include.
- Result/evidence: new doctrine file `docs/SKILL_ASSETIZATION_TRIGGER_RULE.md` now exists and gives OpenClaw an explicit promotion path from recurring correction -> reusable operating asset.
- OpenClaw capability improved: stronger knowledge compounding and lower risk of losing workflow lessons to chat drift or daily-memory-only storage.
- Guardrail learned: if a correction still matters after restart and changes execution behavior, chronology alone is not enough; it needs doctrine or skill promotion.
- Yangming check: yes — the idea of skill-based learning was converted into a concrete doctrine artifact in the same day.
- Next improvement: turn one high-frequency workflow, likely heartbeat execution, into a full `SKILL.md` with trigger conditions, evidence checks, and blocked/degraded handling.
