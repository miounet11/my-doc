## iOS legacy-profile compatibility path was added without polluting the branded default

- Action taken:
  - added `projects/chat-mirror/scripts/configure-ios-release.mjs`
  - added `package-shell.sh configure-ios-legacy` and `configure-ios-default`
  - documented the compatibility route in `docs/APPLE_RELEASE_READINESS.md` and `docs/PACKAGING.md`
- Verified result:
  - `legacy-milichat` mode switches the iOS target bundle id to `com.qs.mlchat`, sets manual signing, injects `PROVISIONING_PROFILE_SPECIFIER = milichat`, updates `capacitor.config.json`, and rewrites `ExportOptions.plist` with manual provisioning profile mapping
  - `default` mode restores the branded `com.aibrowseratlas.mobile` state and removes the legacy profile binding
- Guardrail learned:
  - for private or transitional Apple signing assets, the right path is not to permanently rename the product to match old certificates; it is to provide an explicit reversible compatibility mode for test exports.

## AibrowserAtlas download center now ships a public legacy iOS source bundle and the live release matrix was refreshed

- Action taken:
  - extended `projects/chat-mirror/scripts/configure-ios-release.mjs` with `--root` support so iOS signing modes can be applied to temp staging roots
  - upgraded `projects/chat-mirror/scripts/publish-downloads.mjs` to build two iOS source archives:
    - branded default `AibrowserAtlas-1.0.1-ios-project.zip`
    - legacy-compatible `AibrowserAtlas-1.0.1-ios-legacy-project.zip`
  - updated the download catalog, install guide, acceptance gate, and fallback download metadata to expose the new legacy iOS bundle truthfully
  - deployed the updated release matrix to production and refreshed acceptance at `projects/chat-mirror/data/acceptance/20260317T010403Z.json`
- Verified result:
  - live `https://chat.aibrowseratlas.com/api/downloads` now returns 13 entries, including both `ios-project-source` and `ios-project-source-legacy`
  - live `https://chat.aibrowseratlas.com/ios-install.html` now explains the standard branded path versus the `milichat` legacy signing path
  - direct legacy bundle URL returns HTTP 200 with the new archive
  - production smoke recovered to `{"ok":true,"version":"1.0.1","releaseStage":"v1","releaseChannel":"stable"}` after redeploy
  - `git push` initially hung until GitHub CLI credential wiring was enabled with `gh auth setup-git`; after that, `main` pushed successfully to `https://github.com/miounet11/myjizuo.git`
- Guardrail learned:
  - if a private GitHub remote hangs on this host while `gh auth status` is already healthy, run `gh auth setup-git` before treating push as blocked; the missing piece can be git credential helper wiring rather than account auth itself.

## iOS signing materials are now complete on this Mac except for full Xcode, and Apple doctor now reflects the active bundle id honestly

- Action taken:
  - imported `/Users/lu/Downloads/signature_all/ios/mili.p12` into the login keychain using the provided password from `ios备注.md`
  - installed the provisioning profile into `~/Library/MobileDevice/Provisioning Profiles/d46f2554-fda0-42ae-8900-18f7e5ca4bfc.mobileprovision`
  - fetched and imported Apple’s official `AppleWWDRCAG3.cer` intermediate so the `Apple Distribution` certificate chain becomes valid
  - patched `projects/chat-mirror/scripts/apple-release-doctor.sh` so it reads the active iOS bundle id from `ios/App/App/capacitor.config.json` when present and reports both `Developer ID Application` and `Apple Distribution` visibility
- Verified result:
  - `security find-identity -v -p codesigning "$HOME/Library/Keychains/login.keychain-db"` now shows `Apple Distribution: Kunshan Xingwang Shuke Network Technology Co., Ltd. (2B7JZ4N26U)`
  - in branded default mode, doctor correctly reports profile mismatch against `com.aibrowseratlas.mobile`
  - in `legacy-milichat` mode, doctor correctly reports profile match against `com.qs.mlchat`
  - the only remaining iOS packaging blocker on this host is still the absence of full Xcode: `xcode-select -p` remains `/Library/Developer/CommandLineTools`
- Guardrail learned:
  - for Apple mobile signing, `.cer` plus `.mobileprovision` is still not enough; the usable turning point is when the `.p12` private key is imported and the matching Apple intermediate certificate is trusted, otherwise `find-identity` will stay empty even though the leaf certificate exists.

## Full Xcode install path is prepared, but Apple rejected the credentials from `ios备注.md`

- Action taken:
  - installed `mas` successfully and verified the Mac App Store listing for `Xcode 26.3`
  - downloaded a prebuilt `xcodes 1.6.2` binary into `/Users/lu/.local/bin/xcodes`, avoiding the Homebrew source-build failure on hosts without full Xcode
  - verified `xcodes list` works and sees current Xcode releases including `26.3`
  - attempted `xcodes install 26.3 --directory /Users/lu/Applications --no-superuser --experimental-unxip`
  - when prompted, used the Apple ID values implied by `signature_all/ios/ios备注.md`: user `632044343@qq.com`
- Verified result:
  - `xcodes` progressed to the Apple authentication prompt correctly, so the local install path is viable without `sudo`
  - Apple returned `Invalid username and password combination` for the provided credentials, so the current blocker is account auth, not disk, network, certificates, provisioning, or installer tooling
  - `/Users/lu/Applications` has sufficient free space for a full Xcode install
- Guardrail learned:
  - once `xcodes` is available locally, the final Xcode blocker can collapse to pure Apple account truth; do not keep blaming host tooling after Apple has already accepted the install command and only rejected login credentials.

## Public-ready macOS-only Ghost OS adapter skeleton was created for OpenClaw

- Action taken:
  - created `projects/macos-only/` as a publishable repo skeleton for `https://github.com/miounet11/macOS-only`
  - added public docs covering architecture, install/permissions, routing/verification, and recipe strategy
  - copied and adapted the workspace `skills/ghost-desktop` skill into `projects/macos-only/skill/ghost-desktop/`
  - added `projects/macos-only/scripts/verify-ghost-os.sh` and a skill-local wrapper `ghost-check.sh`
  - added `projects/macos-only/examples/workflows.md` with realistic routing examples
- Verified result:
  - `bash -n` passed for both shell scripts
  - `projects/macos-only/scripts/verify-ghost-os.sh` ran on this Mac and truthfully reported `status=blocked reason=ghost-not-installed`
  - the skill wrapper `projects/macos-only/skill/ghost-desktop/scripts/ghost-check.sh` produced the same blocked result
- Guardrail learned:
  - for public adapter repos, the right claim is adapter readiness plus explicit host verification status, not fake end-to-end success when the execution binary is missing.

## AibrowserAtlas Apple packaging was normalized onto `com.qs.mlchat`, and the live mobile entry was corrected back to the working chat domain

- Action taken:
  - switched the Apple-facing package defaults in `projects/chat-mirror` onto the provided `com.qs.mlchat` / `milichat` path for macOS and iOS
  - rebuilt the macOS desktop package with a real `Developer ID Application` identity by creating a dedicated build keychain that grants non-interactive `codesign` access
  - removed the public legacy iOS download track, regenerated the download catalog, and kept the iOS source bundle aligned to `com.qs.mlchat`
  - discovered that `https://www.aibrowseratlas.com/chat` is still `404` in production, so moved the mobile shell entry back to `https://chat.aibrowseratlas.com`
  - rebuilt Android artifacts and republished downloads so Android release APK and iOS project source both point to the working chat domain
  - redeployed production twice and reran full public acceptance
- Verified result:
  - `apps/aibrowseratlas-shell/dist/mac-arm64/AibrowserAtlas.app` now reports `Identifier=com.qs.mlchat` and `Authority=Developer ID Application: Kunshan Xingwang Shuke Network Technology Co., Ltd. (2B7JZ4N26U)`
  - `spctl -a -vv` truthfully reports `source=Unnotarized Developer ID`, which matches the current notarization gap
  - live chat homepage now serves the latest asset hash `index-Brc17wQ0.js`
  - live downloads now expose:
    - Android release APK built at `2026-03-17T05:17:46.048Z`
    - iOS source bundle built at `2026-03-17T05:18:25.657Z`
    - macOS DMG signed build at `2026-03-17T05:09:00.006Z`
  - formal acceptance passed again and refreshed `projects/chat-mirror/data/acceptance/20260317T052202Z.json` plus `latest.json`
- Guardrail learned:
  - do not ship a mobile shell against a fallback domain just because it is planned; verify the exact public URL first. A live `404` on the shell entry is a higher-priority release blocker than the original WebView workaround.

## macOS notarization moved from “missing issuer” to “API credentials do not authenticate”

- Action taken:
  - rechecked `projects/chat-mirror/scripts/apple-release-doctor.sh` and `scripts/notarize-mac.sh`
  - verified the current signed DMG still exists at `apps/aibrowseratlas-shell/dist/AibrowserAtlas-1.0.1-mac-arm64.dmg`
  - confirmed the only App Store Connect API private key currently present on disk is `/Users/lu/Downloads/signature_all/ios/AuthKey_AH52399668.p8`
  - attempted notarization using:
    - key file: `AuthKey_AH52399668.p8`
    - key id: `AH52399668`
    - issuer id: `147d3592-7cc0-4c95-a064-a4c9f04b4aff`
- Verified result:
  - prereq checks passed locally
  - actual `xcrun notarytool submit ... --wait` failed with `HTTP status code: 401. Unauthenticated`
  - no local file matching the newly provided key id `P78732PRV7` was found under `/Users/lu`
- Guardrail learned:
  - `APPLE_NOTARY_ISSUER` alone is not the missing piece anymore. The remaining blocker is the matching App Store Connect `.p8` private key for the active key id. The mac `.p12` signing certificate is not a substitute for notarization API auth.

## AibrowserAtlas notarization auth is fixed, hardened runtime is enabled, and the remaining blocker is Apple queue time

- Action taken:
  - switched the desktop shell build to `hardenedRuntime: true`
  - updated `scripts/notarize-mac.sh` and `scripts/apple-release-doctor.sh` to default to the working App Store Connect API key:
    - key file: `/Users/lu/Downloads/AuthKey_P78732PRV7.p8`
    - key id: `P78732PRV7`
    - issuer id: `147d3592-7cc0-4c95-a064-a4c9f04b4aff`
  - rebuilt the mac shell and verified the signed app now carries `flags=0x10000(runtime)` under `codesign -dv`
  - submitted the rebuilt DMG to Apple notarization as submission id `0230644a-840d-40c0-a82d-72df441a7891`
  - updated the download publishing path and public desktop/release pages so mac trust/install wording no longer hardcodes “not notarized” outside the real download manifest state
  - deployed the updated site/runtime to production and verified:
    - remote smoke returned `ok`
    - public `/api/downloads` refreshed to `generatedAt=2026-03-17T06:56:04.919Z`
- Verified result:
  - auth is no longer the blocker; `notarytool` accepts the new credentials
  - the previous validation failure root cause (missing hardened runtime) is fixed in the rebuilt app
  - as of this note, Apple still reports the active submission as `In Progress`; local packaging and production deployment are complete, but stapling cannot proceed until Apple finishes processing
- Guardrail learned:
  - after notarization auth and runtime signing are correct, the next blocker can be pure Apple-side queue latency. Do not resubmit duplicate archives just because `In Progress` runs long; hold the submission id and wait for a terminal state before stapling or republishing as notarized.
