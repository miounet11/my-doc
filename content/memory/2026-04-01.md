# 2026-04-01

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `skills/heartbeat-closure/SKILL.md`, `docs/HARNESS_READINESS_AUDIT.md`, `docs/HARNESS_CHECKLIST.md`, `docs/HARNESS_QUICKSTART.md`, `docs/HARNESS_INDEX.md`, `docs/HARNESS_STATE_MAP.md`, `docs/HARNESS_STARTUP_BOOTSTRAP.md`, `docs/HARNESS_SKILL_SET.md`, `docs/HARNESS_ASSET_REGISTRY.md`, `docs/HARNESS_REVIEW_LOOP.md`.
- Hypothesis: after several runtime-profile repairs, the review doctrine itself might still be missing a question that explicitly checks for skill-only routing drift versus the new profile-aware control layer.
- Evidence found: `docs/HARNESS_REVIEW_LOOP.md` asked about doctrine/skill/template promotion, but it did not explicitly ask reviewers to catch support docs that still describe routing in skill-only terms after runtime profiles became a first-class layer.
- Action taken: updated `docs/HARNESS_REVIEW_LOOP.md` to add a review question for skill-only-routing drift and to treat runtime profile binding as a promotion target.
- Result/evidence: future harness review passes now have an explicit check for profile-awareness drift, making it less likely that the new control layer will quietly fall out of support docs or review habits.
- OpenClaw capability improved: stronger harness observability and better self-review coverage for the runtime-profile layer.
- Guardrail learned: once a control layer becomes real, review doctrine must test for its omission explicitly or drift just reappears one support file at a time.
- Yangming check: yes — repeated repair history was turned into a stronger review question instead of staying only as ad hoc cleanup.
- Next improvement: if more profile-aware routing corrections keep recurring, promote a compact dedicated audit checklist for runtime-profile binding across docs/skills.

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `skills/heartbeat-closure/SKILL.md`, `docs/HARNESS_QUICKSTART.md`, `docs/HARNESS_CHECKLIST.md`, `docs/RUNTIME_PROFILES.md`.
- Hypothesis: one remaining support reference still likely assumed skill-only routing, even after runtime profiles were installed and explicitly wired into quickstart/bootstrap.
- Evidence found: `docs/HARNESS_QUICKSTART.md` already points to runtime profiles, but `docs/HARNESS_CHECKLIST.md` still had a `Skill Routing` section that named only skills and omitted profile selection for exploration, implementation, verification, recovery, and heartbeat posture.
- Action taken: updated `docs/HARNESS_CHECKLIST.md` so the routing section now includes profile selection alongside skill routing.
- Result/evidence: the checklist now matches the installed harness model more closely, so quick support checks reinforce both the correct skill and the correct runtime posture.
- OpenClaw capability improved: stronger execution clarity and better profile-aware routing during fast completion/review decisions.
- Guardrail learned: once a new control layer exists, fast-path support docs must mention it too or the harness silently falls back to the older mental model under time pressure.
- Yangming check: yes — a noticed routing-model mismatch was turned into a concrete checklist repair.
- Next improvement: check whether any remaining support or audit docs still discuss routing purely in terms of skills without profile posture.

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `skills/heartbeat-closure/SKILL.md`, `docs/HARNESS_INDEX.md`, `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md`, `docs/HARNESS_READINESS_AUDIT.md`, `docs/HARNESS_CHECKLIST.md`, `docs/HARNESS_SKILL_SET.md`.
- Hypothesis: one support-layer file may still describe the harness skill set in an outdated earlier phase, which would weaken gardening and readiness review by suggesting several installed closure skills are still only planned.
- Evidence found: `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md` still listed only the older six first-class skills and treated `acp-task-closure`, `github-delivery-closure`, and `deploy-readiness` as merely planned, despite the current active skill set in `docs/HARNESS_SKILL_SET.md` already including those and several newer closure skills.
- Action taken: updated `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md` so its active first-class skill list matches the currently installed harness skills and replaced the stale planned-skills section with an explicit `none right now` note.
- Result/evidence: the support/gardening reference now matches current harness reality, reducing stale-phase drift during future cleanup and review passes.
- OpenClaw capability improved: stronger harness observability and less outdated routing/support guidance.
- Guardrail learned: support docs that classify active assets must age with the live skill graph, or they quietly reintroduce historical-evidence smuggling into maintenance work.
- Yangming check: yes — stale classification doctrine was converted into a concrete file repair instead of being left as a noticed inconsistency.
- Next improvement: audit whether any other support docs still assume only skill-based routing and should mention the runtime-profile layer explicitly.

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `skills/heartbeat-closure/SKILL.md`, `docs/HARNESS_READINESS_AUDIT.md`, `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md`, `docs/HARNESS_ASSET_REGISTRY.md`, `docs/HARNESS_SKILL_SET.md`, `docs/RUNTIME_PROFILES.md`.
- Hypothesis: after adding runtime profiles to registry and readiness audit, the support-asset classification notes might still lag behind, leaving the new doctrine file unclassified in the harness cleanup/reference layer.
- Evidence found: `docs/RUNTIME_PROFILES.md` is now a routed support asset and is counted by readiness/inventory surfaces, but `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md` did not list it among the assets that should remain support docs rather than become first-class skills.
- Action taken: updated `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md` to include `docs/RUNTIME_PROFILES.md` in the support-assets-not-skills list.
- Result/evidence: the support/reference layer now classifies runtime profiles consistently with the rest of the harness, reducing the chance of skill-layer inflation or future misclassification.
- OpenClaw capability improved: stronger harness gardening consistency and cleaner separation between workflow skills and support doctrine.
- Guardrail learned: when a new support-layer doctrine file becomes important, classify it explicitly or future cleanup/review passes can drift on whether it belongs in routing, support docs, or the skill layer.
- Yangming check: yes — a small but real classification drift was converted into a concrete harness-gardening repair.
- Next improvement: review whether any support docs still describe the active first-class skill set in outdated terms now that several newer closure skills exist.

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `docs/HARNESS_INDEX.md`, `docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`, `skills/heartbeat-closure/SKILL.md`, `docs/HARNESS_ENGINEERING_UPGRADE_PLAN.md`, `skills/harness-gardening/SKILL.md`, `docs/HARNESS_REVIEW_LOOP.md`, `docs/HARNESS_CHECKLIST.md`, `docs/HARNESS_UPGRADE_LADDER.md`, `docs/HARNESS_ANTI_PATTERNS.md`, `docs/HARNESS_RETIRE_MERGE_CANDIDATES.md`, `docs/HARNESS_STATE_MAP.md`, `docs/HARNESS_READINESS_AUDIT.md`, `docs/RUNTIME_PROFILES.md`.
- Hypothesis: after installing the runtime-profile layer and repairing the asset registry, one more audit surface likely still lagged behind — readiness criteria may not yet count runtime profiles as a required support asset, which would leave the audit view weaker than the actual harness.
- Evidence found: `docs/RUNTIME_PROFILES.md` exists and is now routed/bootstrap-visible, but `docs/HARNESS_READINESS_AUDIT.md` support-assets section did not list it among the required installed surfaces.
- Action taken: updated `docs/HARNESS_READINESS_AUDIT.md` so `docs/RUNTIME_PROFILES.md` is explicitly required under support assets.
- Result/evidence: the readiness audit now measures the runtime-profile layer instead of silently omitting it, so audit truth better matches the installed harness structure.
- OpenClaw capability improved: stronger harness observability and more accurate readiness auditing.
- Guardrail learned: when a new control layer is installed, both inventory and audit surfaces must name it or the system can look more complete than the audit actually measures.
- Yangming check: yes — a review-loop gap was turned into a concrete audit repair in the same turn.
- Next improvement: check whether profile-aware routing should also be reflected in any support references that still assume only skill-based workflow selection.

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `docs/HARNESS_INDEX.md`, `docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`, `skills/heartbeat-closure/SKILL.md`, `docs/RUNTIME_PROFILES.md`, `docs/HARNESS_QUICKSTART.md`, `docs/HARNESS_STARTUP_BOOTSTRAP.md`, `docs/HARNESS_ASSET_REGISTRY.md`, `docs/HARNESS_SKILL_SET.md`.
- Hypothesis: the new runtime-profile layer was partially installed — active in quickstart/bootstrap, but still missing from the central harness asset inventory, which weakens inspectability and makes the layer easier to forget during future maintenance.
- Evidence found: `docs/RUNTIME_PROFILES.md` exists and is referenced by `docs/HARNESS_QUICKSTART.md` and `docs/HARNESS_STARTUP_BOOTSTRAP.md`, but `docs/HARNESS_ASSET_REGISTRY.md` did not list it under routing/bootstrap assets.
- Action taken: updated `docs/HARNESS_ASSET_REGISTRY.md` to include `docs/RUNTIME_PROFILES.md` in the routing/bootstrap inventory.
- Result/evidence: the central harness registry now reflects the runtime-profile layer alongside the other bootstrap surfaces, so the installed harness is more inspectable and less likely to drift into half-installed status.
- OpenClaw capability improved: stronger harness observability and better alignment between installed doctrine and inventory surfaces.
- Guardrail learned: when a new control layer is added, its bootstrap references are not enough — the inventory/registry must also name it or the harness silently becomes harder to audit.
- Yangming check: yes — a doctrine gap found during heartbeat review was turned into an explicit registry repair.
- Next improvement: bind profile awareness into more skill docs or routing examples so profile selection shapes not just bootstrap doctrine but live per-skill execution behavior.

- Source files reviewed this run: `docs/HARNESS_INDEX.md`, `docs/HARNESS_QUICKSTART.md`, `docs/HARNESS_STARTUP_BOOTSTRAP.md`, `docs/HARNESS_ENGINEERING_UPGRADE_PLAN.md`, `docs/TASK_TEMPLATE_EXECUTION_PLAN.md`, user-provided Claude Code architecture notes.
- Hypothesis: OpenClaw still lacked one layer that Claude Code uses heavily — explicit runtime profiles that shape execution posture, verification expectations, and lifecycle behavior before a task expands.
- Evidence found: the harness had strong routing/closure assets and a new verification skill, but no single doctrine file defined `explore` / `plan` / `implement` / `verify` / `recover` / `heartbeat` as execution contracts; quickstart and startup bootstrap also did not point to such a layer.
- Action taken: created `docs/RUNTIME_PROFILES.md` defining six execution profiles with posture, evidence expectations, transition rules, and binding rules; updated `docs/HARNESS_QUICKSTART.md` and `docs/HARNESS_STARTUP_BOOTSTRAP.md` so runtime-profile awareness loads early and shapes live work.
- Result/evidence: OpenClaw now has a first-class runtime-profile layer instead of relying only on skills plus generic prompt behavior, and both quickstart/bootstrap docs now explicitly route through that layer.
- OpenClaw capability improved: clearer phase selection, stronger separation of understanding/planning/implementation/verification/recovery work, and a better path for future subagent/ACP/runtime binding.
- Guardrail learned: if profiles do not change tool posture, evidence contract, and lifecycle expectations, they are just labels; explicit profile doctrine is needed to prevent that drift.
- Yangming check: yes — the Claude Code lesson about agent/runtime specialization was turned into a live harness control layer.
- Next improvement: bind existing skills and future ACP/subagent paths explicitly to one primary runtime profile so profile selection affects execution behavior rather than staying only doctrinal.
