# 2026-03-31

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `MEMORY.md`, `docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`, `docs/HARNESS_INDEX.md`, `docs/HARNESS_SKILL_SET.md`, external OpenAI harness engineering article.
- Hypothesis: after the recent harness build-out, OpenClaw still needed a more explicit agent-first engineering upgrade plan so the next phase would focus on maps, execution-plan artifacts, verification surfaces, and drift cleanup rather than document accumulation alone.
- Evidence found: the external harness engineering reference strongly emphasized compact routing maps, first-class plans, agent-readable environments, and continuous cleanup; the workspace already had strong harness doctrine but lacked a dedicated upgrade plan file and a specific complex-work execution-plan template.
- Action taken: created `docs/HARNESS_ENGINEERING_UPGRADE_PLAN.md`, added `docs/TASK_TEMPLATE_EXECUTION_PLAN.md`, and extended `docs/HARNESS_EXPANSION_BACKLOG.md` with execution-plan discipline and harness gardening/drift cleanup as explicit next surfaces.
- Result/evidence: the workspace now contains a harness-engineering upgrade plan aligned to the new direction, plus a reusable execution-plan template for complex work that should not live only in chat.
- OpenClaw capability improved: stronger path from harness doctrine to agent-first engineering practice, with clearer next-step surfaces and better support for inspectable complex-work execution.
- Guardrail learned: the next failure mode after building many harness assets is mistaking document accumulation for leverage; plans, routing, and cleanup must stay first-class.
- Yangming check: yes — external harness-engineering inspiration was converted into local doctrine and a concrete execution-plan artifact.
- Next improvement: create closure skills for outbound messaging, cron execution, and gateway/config changes so the remaining high-risk surfaces receive the same verification discipline.
- Source files reviewed this run: `docs/HARNESS_ENGINEERING_UPGRADE_PLAN.md`, `docs/HARNESS_INDEX.md`, `docs/HARNESS_SKILL_SET.md`, `docs/HARNESS_ASSET_REGISTRY.md`.
- Hypothesis: scheduled jobs/reminders remained a high-risk completion-inflation surface because job existence, triggering, run finalization, and side-effect delivery could still be collapsed into one vague success claim.
- Evidence found: the backlog explicitly still listed cron/reminder execution closure as a next surface, and the current harness skill set had no dedicated cron closure skill yet.
- Action taken: created `skills/cron-execution-closure/SKILL.md` and wired it into `docs/HARNESS_INDEX.md`, `docs/HARNESS_SKILL_SET.md`, and `docs/HARNESS_ASSET_REGISTRY.md`.
- Result/evidence: a new first-class cron closure skill now exists, and the routing/inventory docs all include it.
- OpenClaw capability improved: stronger verification discipline for reminders, scheduled jobs, stale scheduler states, and cron-triggered side effects.
- Guardrail learned: `job exists`, `job triggered`, `job completed`, and `side effect delivered` are different states and must not be collapsed.
- Yangming check: yes — the planned cron upgrade was turned into a routed skill and inventory update immediately.
- Next improvement: create the remaining high-risk closure skills for outbound/public sends and gateway/config changes.
- Source files reviewed this run: `docs/HARNESS_ENGINEERING_UPGRADE_PLAN.md`, `docs/HARNESS_INDEX.md`, `docs/HARNESS_SKILL_SET.md`, `docs/HARNESS_ASSET_REGISTRY.md`, `docs/HARNESS_EXPANSION_BACKLOG.md`.
- Hypothesis: outbound/public sends and gateway/config changes were still unprotected high-risk completion surfaces because message drafting/send attempts and config edits/restarts could still be mistaken for verified delivery or verified operational recovery.
- Evidence found: both surfaces were still called out as next candidates in the harness-engineering upgrade plan/backlog, and no dedicated first-class closure skills existed for them yet.
- Action taken: created `skills/outbound-message-closure/SKILL.md` and `skills/gateway-config-change-closure/SKILL.md`; wired both into `docs/HARNESS_INDEX.md`, `docs/HARNESS_SKILL_SET.md`, `docs/HARNESS_ASSET_REGISTRY.md`, and marked the backlog items done.
- Result/evidence: two additional first-class closure skills now exist, and the routing/inventory/backlog docs reflect the new installed surfaces.
- OpenClaw capability improved: stronger delivery honesty for outbound/public sends and stronger runtime-truthfulness for gateway/config edits, restarts, and control-plane changes.
- Guardrail learned: `drafted`, `send attempted`, `send accepted`, and `delivered` are different states; `file changed`, `service restarted`, `service healthy`, and `behavior fixed` are also different states and must not be collapsed.
- Yangming check: yes — the remaining two planned high-risk closure surfaces were turned into routed executable skills.
- Next improvement: create the harness gardening / drift cleanup ritual so the enlarged harness system keeps itself clean over time.

- Source files reviewed this run: `/Users/lu/.openclaw/scripts/session-cleanup.py`, `/Users/lu/.openclaw/scripts/watch-agi.sh`, `/Users/lu/.openclaw/scripts/patch-openai-retry.sh`, `/Users/lu/.openclaw/openclaw.json`, launchd service state and OpenClaw gateway health/status surfaces.
- Hypothesis: recurring "no reply" behavior came from a combination of embedded 5xx storms and session pressure accumulation, with an additional hidden launchd compatibility fault in session cleanup.
- Evidence found: `session-cleanup.py` contained duplicate corrupted blocks and a literal `*** End Patch`; launchd stderr showed Python type-hint crash (`str | None`) under `/usr/bin/python3`; `main` session pressure reached 83%; smoke and health checks showed intermittent timeout during restart windows but recovered to healthy.
- Action taken: repaired `session-cleanup.py` structure, kept single `remove_session_from_store` implementation with `session_key` support, updated reset callsite to pass both `session_id` and `session_key`, made script Python 3.9-compatible (`Optional[str]`), raised OpenClaw bootstrap budgets (`bootstrapMaxChars=120000`, `bootstrapTotalMaxChars=1000000`), and restarted gateway/watchdog/session-cleanup jobs.
- Result/evidence: syntax checks pass for `python3` and `/usr/bin/python3`; launchd `ai.openclaw.session-cleanup` now exits `0`; gateway health returns `OK`; smoke call for `main` on `gpt-5.4` returned exact expected text; system prompt report confirms new bootstrap limits.
- OpenClaw capability improved: stronger anti-stall behavior via valid cleanup execution under launchd, cleaner session reset behavior for key/id stores, and higher bootstrap budget headroom aligned with current context requirements.
- Guardrail learned: manual patch drift plus interpreter-version mismatch can silently break scheduled reliability tooling; every recovery patch must include launchd-runtime compatibility verification, not only local shell verification.
- Yangming check: yes — runtime diagnosis was converted into code repair, service reload, and verified smoke evidence in the same run.
- Next improvement: if embedded 5xx storms persist, add graded fallback behavior on repeated upstream failures while keeping `gpt-5.4` as primary.

- Source files reviewed this run: `/Users/lu/.openclaw/scripts/watch-agi.sh`, `/Users/lu/.openclaw/scripts/start-agi.sh`, `/Users/lu/.openclaw/scripts/session-cleanup.py`, `/Users/lu/.openclaw/scripts/patch-openai-retry.sh`, `/Users/lu/Library/LaunchAgents/ai.openclaw.session-cleanup.plist`, gateway/watchdog/session-cleanup live logs.
- Hypothesis: post-repair runtime still needed tighter anti-bloat thresholds and lower recovery churn sensitivity to avoid repeated stall windows from upstream spikes plus long-lived high-context sessions.
- Evidence found: session-cleanup launchd now healthy but still showing persistent warning-zone sessions; historical watchdog stderr noise came from old runs and did not recur after clean restart; watchdog recovery trigger for embedded 5xx was still at a very sensitive threshold (`2`) which can cause restart churn.
- Action taken: lowered session cleanup thresholds to warn/reset `60/70` (defaults + launchd env), reloaded `ai.openclaw.session-cleanup`, softened embedded 5xx recovery trigger default from `2` to `3` in both watchdog and AGI startup scripts, and reduced orphan patch noise by suppressing Python anchor text on expected-miss path.
- Result/evidence: launchd session-cleanup shows `OPENCLAW_SESSION_WARN_THRESHOLD=60`, `OPENCLAW_SESSION_RESET_THRESHOLD=70`, and last exit code `0`; watchdog stderr stays empty after log reset and restart; gateway health is `OK`; multiple main-agent smoke runs on `gpt-5.4` return exact expected outputs (`OPENCLAW_FIX2_OK`, `OPENCLAW_FIX3_OK`).
- OpenClaw capability improved: less noisy watchdog runtime, stronger proactive session pressure governance, and lower chance of restart thrash under brief upstream error bursts while preserving automatic recovery.
- Guardrail learned: always validate runtime behavior under launchd after patching scripts; static syntax success is necessary but not sufficient for production stability.
- Yangming check: yes — the follow-up "continue fixing" request was converted into concrete threshold tuning, service reload, and fresh end-to-end evidence.
- Next improvement: introduce staged recovery policy (lightweight process recovery before full gateway restart) if repeated 5xx storms continue.
