# 2026-03-10

## External Soul Pack Review

- Reviewed `/Users/lu/Downloads/dan-koe-mentor-souls` as an imported soul-pack example.
- Conclusion: the pack is useful as a packaging reference, not as a direct doctrine import for the OpenClaw main brain.
- Strong parts worth extracting:
  - complete role skeleton (`SOUL/IDENTITY/AGENTS/USER/TOOLS/MEMORY/BOOTSTRAP/HEARTBEAT`)
  - strict domain boundaries
  - structured role-local memory
  - compound review / nightly review pattern
  - failure transparency and trust-recovery rules
- Parts explicitly rejected for main-brain use:
  - heavy persona transfer
  - mentor-specific proactive cadence
  - single-agent privacy model as a system-wide default
  - worldview/framework content as if it were engineering doctrine
- New artifacts created:
  - `docs/OPENCLAW_AGENT_PACKAGING_RULES.md`
  - `scaffolds/mentor-agent/*`
- System decision:
  - main/system agents learn the packaging method only
  - mentor-style persona is isolated into a dedicated scaffold

## Completion Return Path Repair

- Stopped treating `completion-return-path` as doctrine-only; pushed it into live execution artifacts for `projects/原始`.
- Added `scripts/openclaw-completion-return-audit.sh` and wired it into `projectctl` and `openclaw-complex-cycle.sh`.
- Fixed a real execution-layer gap:
  - old `complex-cycle` aborted on worker failure before writing closure artifacts
  - new flow always carries execution through completion-return audit and summary generation
- Upgraded `scripts/run-claude-brief.sh` status outputs to align with `projects/原始/docs/WORKER_STATUS_SCHEMA.md`, including:
  - `worker_name`
  - `execution_surface`
  - `artifact_paths`
  - `outcome_class`
  - `openclaw_review_status`
  - `openclaw_judgment`
  - `next_action`
- Ground-truth validation:
  - iteration 18 now shows the real old defect clearly:
    - `internal_execution_state=completed-success`
    - `requester_return_state=review-missing`
    - `overall=degraded`
  - iteration 19 live worker artifacts already show schema-aligned status during execution; `architect` reached `succeeded` with full evidence fields while `coder` was still running.

## Loop 19 Closure And Contamination Isolation

- Completed `loop-19.md` and `loop-19-reflection.md` for `projects/原始`.
- Added `projects/原始/docs/CONTAMINATION_CLEANUP_PLAN.md` to isolate historical false-fix narratives instead of letting them leak into current-state claims.
- Updated `projects/原始/docs/EVIDENCE_BOARD.md` so it now reflects:
  - iteration 19 execution success
  - completion-return closure success
  - current readiness recovered to `overall=ok`
  - historical `readiness_overall=degraded` in orchestration summary treated as a point-in-time snapshot, not current state
- Key system lesson:
  - current-state evidence, historical snapshots, and contamination sources must remain separate categories
  - otherwise the system reintroduces fake progress by mixing repaired present state with polluted old narratives

## Supervision Anchor Repair

- Repaired `scripts/openclaw-supervise-codex.sh` so supervision now anchors to:
  - latest loop report by iteration number
  - latest loop reflection by iteration number
  - `projects/原始/docs/EVIDENCE_BOARD.md`
  - `projects/原始/docs/CONTAMINATION_CLEANUP_PLAN.md`
- This fixed a real bug where newly edited historical files (`loop-15/16`) could outrank `loop-19` by mtime and be mistaken for the latest loop.
- Verified with fresh supervision run:
  - `codex-supervision-20260310-082119/summary.md` now points to `loop-19.md` and `loop-19-reflection.md`

## Heartbeat current-state vs historical-snapshot split

- Fresh heartbeat readback confirms the system has crossed an important evidence boundary for `projects/原始`.
- Source files reviewed:
  - `projects/原始/docs/EVIDENCE_BOARD.md`
  - `projects/原始/data/loops/loop-19.md`
  - `projects/原始/data/orchestration/iteration-19-20260310-032140/browser-smoke-evidence.md`
  - `projects/原始/docs/CONTAMINATION_CLEANUP_PLAN.md`
  - `projects/原始/data/retrospectives/codex-supervision-20260310-082119/summary.md`
  - `data/system/readiness-last.json`
- Action taken:
  - re-verified that current readiness and historical iteration-19 degraded snapshot are now explicitly separated in project evidence
- Result / evidence:
  - `data/system/readiness-last.json` now shows `overall=ok` and `browser_smoke=ok`
  - `loop-19.md` still correctly keeps `full-chain verified = no` for the original iteration snapshot
  - `browser-smoke-evidence.md` explains that the missing browser artifact was a historical evidence persistence gap, not a worker execution failure
  - `EVIDENCE_BOARD.md` now states both truths together without mixing them
- OpenClaw capability improved:
  - judgment quality around repaired-present-state vs degraded-historical-snapshot
- Guardrail learned:
  - once a degraded historical snapshot is formally explained and current readiness is separately verified, the system should stop treating that old snapshot as the current blocker
- Yangming check:
  - yes; the distinction is no longer just analysis text, it is now reflected in loop closure, evidence board, cleanup plan, and current readiness artifact
- Next improvement:
  - keep contamination cleanup focused on source polluted texts and unstable session-inspection claims, not on already-closed readiness evidence

## Main Growth Heartbeat

- 本轮按 HEARTBEAT 要求重新核查了学习协议、重构方案、AGI 文档、记忆树、Codex supervision、autonomy protocol，并用最新现场产物替代旧叙事。
- 新确认的关键事实：`projects/原始/data/orchestration/iteration-19-20260310-032140/summary.md` 与 `completion-return.md` 已证明第 19 轮达到：
  - `architect_run_status=succeeded`
  - `coder_run_status=succeeded`
  - `completion_return_overall=ok`
  - `requester_return_state=ready`
  - `internal_execution_state=completed-success`
- 同时也确认了仍然存在的缺口：
  - `projects/原始/data/loops/loop-19.md` 仍缺失，说明 retrospective / loop 收口还没完全补齐
  - 证据板 `projects/原始/docs/EVIDENCE_BOARD.md` 还停留在 iteration 17，需要升级到能反映 iteration 19 的真实状态
- 因而本轮最具体、最高杠杆的成长结论不是继续泛化讨论，而是：
  - 第 19 轮已经把“completion-return-path 进入 live execution”从未验证推进到了已验证
  - 下一轮主控重点应从“证明 return path 是否打通”切到“把 iteration 19 的真实结论同步进证据板与 retrospective，避免系统记忆继续滞后现场”

## Heartbeat evidence update

- 按 `HEARTBEAT.md` 执行了一次真实的 Codex supervision：`./scripts/projectctl.sh supervise-codex 原始`。
- 新产物：`projects/原始/data/retrospectives/codex-supervision-20260310-042429/summary.md` 与 `summary-structured.md`。
- 这次 heartbeat 做成的具体改进不是空泛复述，而是把 `projects/原始/docs/EVIDENCE_BOARD.md` 从 iteration 17 更新到 iteration 19 证据状态。
- 写入依据：
  - `projects/原始/data/orchestration/iteration-19-20260310-032140/summary.md`
  - `projects/原始/data/orchestration/iteration-19-20260310-032140/completion-return.md`
- 当前最准确结论：
  - 已验证：iteration 19 的 `architect + coder + completion return ready`
  - 未验证完成：`loop-19.md` retrospective 仍缺，`readiness_overall` 仍为 `degraded`，因此不能写成“全链路完全健康”
- Yangming check:
  - 知识是否变成行动：是。本轮没有停留在“知道该证据优先”，而是实际更新了证据板并留下新的 supervision 产物。

## Heartbeat degraded-readiness confirmation

- 再次按 HEARTBEAT 源文件核查 iteration 19 的现场状态，没有复用旧叙事。
- 直接证据：
  - `projects/原始/data/orchestration/iteration-19-20260310-032140/readiness.json` 仍为 `overall=degraded`
  - `projects/原始/data/loops/loop-19.md` 仍不存在（ENOENT）
- 因此本轮新增的具体、证据化结论是：
  - 现在最真实的缺口已经不是 completion-return path，而是 `loop-19` retrospective 缺失 + browser/readiness 面仍 degraded
  - 这说明下一步最高杠杆动作应是补 iteration 19 retrospective / loop 收口，而不是重复证明 worker 完成
- Guardrail learned:
  - 同一个项目即使主执行成功，也不能因为 `completion_return=ready` 就跳过 readiness 面与 retrospective 面的未完成项。

## Heartbeat degraded-surface clarification

- 又做了一次证据核查，重点不是重复旧结论，而是把 `readiness.degraded` 进一步拆清。
- 直接证据：
  - `smoke.log` 显示：`smoke ok: 原始 service and pipeline assets present`
  - `readiness.json` 仍显示：`browser_smoke=degraded`, `overall=degraded`
  - `browser_smoke.log` 文件本身不存在（ENOENT），说明当前 browser 降级面虽被标记出来，但其单独证据文件并未沉淀在预期路径
- 这次 heartbeat 的具体增量结论：
  - 服务与 pipeline 资产 smoke 面是好的
  - 当前 remaining degraded 更集中在 browser smoke / 证据沉淀面，而不是项目主服务本身
- Next improvement:
  - 后续最高杠杆修补点应是让 browser degraded 拥有稳定、可读回的独立证据文件，避免 readiness 只给状态不给足够取证路径。

## Heartbeat ACP-discipline clarification

- 新增核查了 iteration 19 的 `session-spawn-audit.json`、`autonomy-guard.json`、`autonomy-evidence.json`，避免继续沿用错误 ACP 叙事。
- 直接结论：
  - `autonomy-guard.json` 当前是 `overall=ok`，说明主控自治守卫本身没有报新故障。
  - 但 `session-spawn-audit.json` 是 `overall=degraded`，并明确抓到了大量历史错误模式：反复出现 `sessions_spawn(runtime="acp", task="native_host_persist", payload=...)` 这类**违反当前 ACP 协议**的写法。
  - 这与 `AUTONOMY_PROTOCOL.md` / `AGENT_LEARNING_PROTOCOL.md` 一致：`payload=...` 和 `task="native_host_persist"` 都属于无效示例，不应再被当成下一步计划。
- 这次 heartbeat 的具体改进不是修代码，而是把一个长期模糊点钉死：
  - 后续不能再把“伪 ACP 描述”当成有效升级方案
  - 任何 ACP 下一步都必须回到允许字段：`task/runtime/agentId/thread/mode/cwd/label/streamTo`
- Guardrail learned:
  - 不仅结果要证据化，**升级方案本身也必须是合法可序列化的执行描述**，否则只是新一层假行动。

## Heartbeat durable doctrine update

- 这轮按 HEARTBEAT 做的具体动作是把上一轮确认的 ACP 执行描述纪律，正式提升写入 `MEMORY.md`，不再只留在当天日志里。
- 依据文件：
  - `docs/ACP_COMPLEX_WORKFLOW_PROTOCOL.md`
  - `docs/AUTONOMY_PROTOCOL.md`
  - `projects/原始/data/orchestration/iteration-19-20260310-032140/session-spawn-audit.json`
- Durable lesson 已落成：
  - 非法 ACP 描述（`payload=...`、`task="native_host_persist"`、半截 `sessions_spawn(...)`）以后统一视为 doctrine drift / 假行动，而不是候选方案。
- Yangming check:
  - 这次没有只说“应该记住”，而是已经把长期护栏写进了 `MEMORY.md`。

## Heartbeat false-repair-history clarification

- 这轮又做了一个具体取证动作：直接对照 `session-spawn-audit.json` 的 findings，确认历史记录里不仅有“错误 ACP 提议”，还混有早先**错误宣称已修复/已验证桥接成功**的世界模型叙事。
- 直接证据：
  - `session-spawn-audit.json` findings 同时命中了大量非法 `payload=...` / `task="native_host_persist"`
  - 也命中了历史 world-model 文本里关于“bridge verified / persistence resolved”的自相矛盾记录
- 新增的具体结论：
  - 当前要防的不只是未来继续写错 ACP descriptor
  - 还要防 **旧的假修复叙事继续污染判断**，因为它们会把后来正确的 evidence-first 结论重新稀释掉
- Next improvement:
  - 后续若再整理协议或证据板，应把“历史错误修复宣称”单独列为 contamination source，而不是只写成一般性 degraded。

## Heartbeat evidence-board contamination rule

- 这轮做的具体改进是：已经把“历史假修复叙事要单独列为 contamination source”的规则，真实写进 `projects/原始/docs/EVIDENCE_BOARD.md`，而不是继续停留在当天日志里。
- 新增到证据板的内容包括：
  - 一条新的总规则：历史“已修复 / 已桥接成功”叙事若与现证冲突，必须单列
  - 一个独立的 `Contamination sources` 区块
  - 把 `session-spawn-audit.json` 标明为 ACP 污染审计入口
- Yangming check:
  - 这次没有只指出污染，而是把证据板结构本身改成了更抗污染的版本。



## Heartbeat engineering-polish board formalization

- Pre-compaction flush: stored the latest durable outcome from a fresh heartbeat pass instead of leaving it only in chat.
- Source files reviewed during the heartbeat:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/docs/AGENT_LEARNING_PROTOCOL.md`
  - `/Users/lu/.openclaw/LEARNING_REDESIGN.md`
  - `/Users/lu/.openclaw/docs/agi-implementation.md`
  - `/Users/lu/.openclaw/docs/agi-roadmap.md`
  - `/Users/lu/.openclaw/memory-tree/core/MEMORY_TREE.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
  - `~/.openclaw/agents/evolution/agent/CLAUDE.md`
  - `~/.openclaw/agents/goal-manager/agent/CLAUDE.md`
  - `~/.openclaw/agents/world-model/agent/CLAUDE.md`
- Concrete action taken:
  - created `docs/ENGINEERING_POLISH_BOARD_2026-03-10.md` as a real execution board for the newly aligned engineering-polish control surface.
- Result / evidence:
  - the board captures four active tracks:
    - Canvas Memory
    - Model Escalation
    - Resource Guardrails
    - Skill Coverage / Skill Health
  - it also fixes iteration-20 prioritization to:
    - P0: Canvas Memory minimum layering + before/after token measurement
    - P0: skill registry maturity split
    - P1: model escalation policy + evidence schema
    - P1: resource guardrail policy for browser/path/write safety
- Durable lesson:
  - **agent prompt alignment is a control-layer gain, not runtime proof; once prompts are upgraded, the very next phase must be an evidence sprint rather than a celebration phase.**
- OpenClaw capability improved:
  - better conversion of high-level architecture absorption into an inspectable engineering execution board.
- Yangming check:
  - yes; this heartbeat produced a concrete artifact rather than only another opinion about what should happen next.
- Next improvement:
  - implement the first P0 on the board with measurable before/after evidence, especially Canvas Memory layering and token comparison.
