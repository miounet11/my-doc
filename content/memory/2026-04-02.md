# 2026-04-02

- Source files reviewed this run: `HEARTBEAT.md`, `docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`, `docs/heartbeat-mirrors/LEARNING_REDESIGN.md`, `docs/heartbeat-mirrors/agi-implementation.md`, `docs/heartbeat-mirrors/agi-roadmap.md`, `docs/heartbeat-mirrors/MEMORY_TREE.md`, `docs/CODEX_SUPERVISION_LOOP.md`, `docs/AUTONOMY_PROTOCOL.md`, `docs/COMPLETION_GATE_PROTOCOL.md`, `docs/VERIFICATION_CONTRACT.md`, `docs/REPAIR_LOOP_PROTOCOL.md`, `docs/HARNESS_INDEX.md`, `skills/mydoc-publish-verification/SKILL.md`, `docs/MYDOC_PUBLISH_GUARDRAIL.md`, `scripts/sync-mydoc-workspace.sh`, `my-doc-site/config/workspace-sync-manifest.json`.
- Hypothesis: the mydoc sync manifest may be too narrow for live publish work because it mirrors `projects/*/docs/**` but omits root-level `projects/*/*.md`, making some project deliverables invisible to the publish path even when users think "publish the project docs" should include top-level project markdown.
- Evidence found: `my-doc-site/config/workspace-sync-manifest.json` currently includes `docs/**/*.md`, `docs/**/*.json`, and `projects/*/docs/**/*.md|json`, but not root-level project markdown such as `projects/原始/README.md` or iteration/status markdown files. That creates a real mirror gap between project artifacts and user-facing publish expectations.
- Action taken: none yet in code; recorded the defect during heartbeat review so the next repair can widen the manifest carefully instead of silently assuming project docs are already publishable.
- Result/evidence: current state is `degraded` for project-level mydoc publish coverage — docs subfolders mirror, but root-level project markdown does not.
- OpenClaw capability improved: stronger observability over the mydoc delivery surface and better separation between historical deploy success and actual artifact coverage.
- Guardrail learned: a successful deploy pipeline is not sufficient evidence if the manifest excludes the artifact class the user expects to publish.
- Yangming check: yes — a publish-surface assumption was turned into an inspectable defect record instead of remaining an unspoken mismatch.
- Next improvement: update the manifest to include selected root-level project markdown (for example `*/README.md`, `*/CHANGELOG.md`, `*/ITERATION*.md`) and then verify the mirrored output before any publish claim.
