## 2026-03-21 — audited remaining OpenClaw repair blockers after heartbeat path/content fixes

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
  - `/Users/lu/.openclaw/workspace/docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`
  - `/Users/lu/.openclaw/workspace/evals/benchmark-runner.sh`
  - `/Users/lu/.openclaw/workspace/evals/regression-check.sh`
  - `/Users/lu/.openclaw/workspace/evals/description-tuner.sh`
  - `/Users/lu/.openclaw/workspace/scripts/openclaw-context-audit.sh`
  - `/Users/lu/.openclaw/workspace/scripts/openclaw-autonomy-guard.sh`
  - `/Users/lu/.openclaw/workspace/scripts/openclaw-capability-audit.sh`
  - `/Users/lu/.openclaw/workspace/scripts/openclaw-readiness.sh`
- Hypothesis:
  - after the heartbeat mirror fix, the next biggest blockers would likely be either stale fake-eval infrastructure or script execution surface mismatches.
- Action taken:
  - verified current OpenClaw version via CLI and session status
  - checked gateway and browser runtime health
  - inspected benchmark/eval scripts and readiness/audit entrypoints
  - attempted live runs of readiness, context audit, autonomy guard, capability audit, memory bank, browser smoke, and codex supervision surfaces
- Result / evidence:
  - OpenClaw runtime is currently `2026.3.13 (61d171a)` and gateway RPC probe is healthy
  - browser smoke passed, but required auto-start recovery (`browser auto-started via direct http control`)
  - memory bank status returned `overall: ok` but `last_indexed_at` is stale (`2026-03-09T03:49:05.500713+00:00`)
  - `evals/benchmark-runner.sh` is still mostly mock data generation (`TODO: Replace with actual OpenClaw agent invocation`, hardcoded aggregate metrics), so weekly benchmark doctrine is not trustworthy yet
  - `scripts/openclaw-context-audit.sh`, `scripts/openclaw-autonomy-guard.sh`, and `scripts/openclaw-capability-audit.sh` are Python files with `#!/usr/bin/env python3`, but failed when invoked with `bash ...`; this exposed a command-surface mismatch risk in wrappers/invocation habits, not a syntax defect in the files themselves
  - web release discovery via Brave search is currently blocked by missing Brave API key
- OpenClaw capability improved:
  - there is now a sharper map of real remaining blockers after the heartbeat repair instead of assuming the system is broadly healthy
- Guardrail learned:
  - a file being present is not readiness; benchmark scripts, audit scripts, and indexing freshness each need execution-path and evidence checks
- Yangming check:
  - yes: the review moved from general suspicion to concrete, runnable blockers with observable evidence
- Next improvement:
  - replace fake benchmark generation with real eval execution, add an explicit mirror-sync path, refresh memory-bank indexing, and standardize audit script invocation so python entrypoints are never run through `bash`

## 2026-03-21 — replaced heartbeat mirror placeholders with readable doctrine copies

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
  - external originals fetched for mirroring:
    - `/Users/lu/.openclaw/docs/AGENT_LEARNING_PROTOCOL.md`
    - `/Users/lu/.openclaw/LEARNING_REDESIGN.md`
    - `/Users/lu/.openclaw/docs/agi-implementation.md`
    - `/Users/lu/.openclaw/docs/agi-roadmap.md`
    - `/Users/lu/.openclaw/memory-tree/core/MEMORY_TREE.md`
- Hypothesis:
  - heartbeat still was not fully clean because workspace mirror targets existed but only contained placeholders, so the path problem was fixed while the content problem remained.
- Action taken:
  - read the five non-workspace doctrine files via shell access
  - replaced the workspace placeholder mirror files with readable copied content
  - kept `HEARTBEAT.md` pointed at the workspace mirror paths so future heartbeat runs stay sandbox-compatible
- Result / evidence:
  - all five heartbeat mirror files now contain real doctrine content instead of placeholder notices
  - heartbeat `Always Read` inputs are now both workspace-readable and content-bearing
- OpenClaw capability improved:
  - heartbeat can now execute its required reading path inside the workspace sandbox without failing at either path reachability or empty stub content
- Guardrail learned:
  - mirror repair is only complete when both access and content fidelity are restored; placeholders are scaffolding, not completion
- Yangming check:
  - yes: the remaining heartbeat defect was closed by replacing stubs with usable doctrine copies
- Next improvement:
  - when practical, add a lightweight sync/update path so mirrored doctrine does not drift from the upstream originals over time

## 2026-03-21 — added readable workspace mirror targets for heartbeat doctrine inputs

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
- Hypothesis:
  - the previous heartbeat fix improved failure honesty but did not yet remove the main executability gap: several required doctrine files still pointed outside the readable workspace sandbox.
- Action taken:
  - created `docs/heartbeat-mirrors/` inside the workspace
  - added readable workspace-side mirror targets for the five non-workspace heartbeat inputs
  - updated `HEARTBEAT.md` to read the workspace mirror paths instead of the unreadable absolute originals
  - added an explicit rule that placeholder mirrors still count as a heartbeat defect until real mirrored content exists
- Result / evidence:
  - heartbeat required inputs now resolve to workspace-readable paths
  - the protocol no longer depends on sandbox-inaccessible absolute paths for its `Always Read` list
  - the remaining open issue is content fidelity, not path readability
- OpenClaw capability improved:
  - heartbeat execution is now structurally runnable inside the workspace sandbox instead of failing at the path layer
- Guardrail learned:
  - fixing failure reporting is not enough; required inputs also need a reachable execution surface
- Yangming check:
  - yes: the heartbeat path defect was pushed from diagnosis into a concrete workspace-side repair
- Next improvement:
  - replace the placeholder mirror files with real mirrored content so heartbeat reads actual doctrine instead of stubs


- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
- Hypothesis:
  - the previous heartbeat fix improved failure honesty but did not yet remove the main executability gap: several required doctrine files still pointed outside the readable workspace sandbox.
- Action taken:
  - created `docs/heartbeat-mirrors/` inside the workspace
  - added readable workspace-side mirror targets for the five non-workspace heartbeat inputs
  - updated `HEARTBEAT.md` to read the workspace mirror paths instead of the unreadable absolute originals
  - added an explicit rule that placeholder mirrors still count as a heartbeat defect until real mirrored content exists
- Result / evidence:
  - heartbeat required inputs now resolve to workspace-readable paths
  - the protocol no longer depends on sandbox-inaccessible absolute paths for its `Always Read` list
  - the remaining open issue is content fidelity, not path readability
- OpenClaw capability improved:
  - heartbeat execution is now structurally runnable inside the workspace sandbox instead of failing at the path layer
- Guardrail learned:
  - fixing failure reporting is not enough; required inputs also need a reachable execution surface
- Yangming check:
  - yes: the heartbeat path defect was pushed from diagnosis into a concrete workspace-side repair
- Next improvement:
  - replace the placeholder mirror files with real mirrored content so heartbeat reads actual doctrine instead of stubs

## 2026-03-21 — fixed heartbeat doctrine to surface unreadable required inputs as a real defect

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`
- Hypothesis:
  - heartbeat runs could falsely look clean because `HEARTBEAT.md` required several absolute paths outside the readable workspace sandbox, but did not explicitly say that unreadable mandatory inputs must be treated as a heartbeat defect.
- Action taken:
  - patched `HEARTBEAT.md` to state that unreadable required paths must not be silently skipped or treated as a clean heartbeat
  - required blocked paths to be recorded in `memory/YYYY-MM-DD.md`
  - tightened the doctrine so the next step is to fix heartbeat inputs or instructions for future executability
- Result / evidence:
  - heartbeat doctrine now distinguishes "nothing needs attention" from "required inputs were inaccessible"
  - future runs have an explicit rule against false `HEARTBEAT_OK` acknowledgements when mandatory files cannot be read
- OpenClaw capability improved:
  - the heartbeat loop is more evidence-honest and less likely to convert sandbox limitations into fake completion
- Guardrail learned:
  - mandatory inputs need explicit failure handling; otherwise tool-boundary mismatches get mislabeled as clean execution
- Yangming check:
  - yes: the defect was repaired in workspace doctrine instead of being left as a conversational caveat
- Next improvement:
  - either mirror the non-workspace doctrine files into a readable workspace path or update startup/heartbeat instructions to point at their readable copies


- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`
- Hypothesis:
  - heartbeat runs could falsely look clean because `HEARTBEAT.md` required several absolute paths outside the readable workspace sandbox, but did not explicitly say that unreadable mandatory inputs must be treated as a heartbeat defect.
- Action taken:
  - patched `HEARTBEAT.md` to state that unreadable required paths must not be silently skipped or treated as a clean heartbeat
  - required blocked paths to be recorded in `memory/YYYY-MM-DD.md`
  - tightened the doctrine so the next step is to fix heartbeat inputs or instructions for future executability
- Result / evidence:
  - heartbeat doctrine now distinguishes "nothing needs attention" from "required inputs were inaccessible"
  - future runs have an explicit rule against false `HEARTBEAT_OK` acknowledgements when mandatory files cannot be read
- OpenClaw capability improved:
  - the heartbeat loop is more evidence-honest and less likely to convert sandbox limitations into fake completion
- Guardrail learned:
  - mandatory inputs need explicit failure handling; otherwise tool-boundary mismatches get mislabeled as clean execution
- Yangming check:
  - yes: the defect was repaired in workspace doctrine instead of being left as a conversational caveat
- Next improvement:
  - either mirror the non-workspace doctrine files into a readable workspace path or update startup/heartbeat instructions to point at their readable copies


- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/SOUL.md`
  - `/Users/lu/.openclaw/workspace/USER.md`
  - `/Users/lu/.openclaw/workspace/MEMORY.md`
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc.sh`
  - `/Users/lu/.openclaw/workspace/memory/2026-03-19.md`
- Hypothesis:
  - The assistant's main degradation is not model weakness but missing context control: user corrections, phase locks, and evidence labels are not being elevated into persistent behavior constraints.
- Action taken:
  - created `docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`
  - codified phase lock, correction promotion, evidence labeling, deliverable surface checks, and self-repair retrieval rules
- Result / evidence:
  - a durable protocol now exists in workspace doctrine for future self-repair and prompt tightening
- OpenClaw capability improved:
  - the system now has an explicit context-control protocol instead of relying on vague intent to "remember better"
- Guardrail learned:
  - memory files alone do not prevent drift; active control rules must sit between retrieved memory and final reply behavior
- Yangming check:
  - yes: the user's criticism about context drift was turned into a concrete protocol file instead of another conversational promise
- Next improvement:
  - wire this protocol into startup/bootstrap behavior and reply-time self-checks so it constrains live execution automatically

## 2026-03-21 — fixed mydoc publish auth fallback so password auth wins when provided

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc.sh`
- Hypothesis:
  - mydoc publish failures were caused by auth selection order: the script preferred SSH agent whenever `SSH_AUTH_SOCK` existed, even when the agent lacked a valid key and `SSHPASS` was explicitly provided.
- Action taken:
  - patched `scripts/publish-mydoc.sh` so `SSHPASS` is preferred over SSH agent auth
  - verified direct password SSH access to `root@172.245.114.89`
  - re-ran public publish successfully for two docs with HTTP 200 verification
- Result / evidence:
  - publish now succeeds when password auth is available even if a broken SSH agent is present
  - this prevents false auth failures caused by environment noise
- OpenClaw capability improved:
  - document publishing is now more robust and matches operator intent when explicit credentials are supplied
- Guardrail learned:
  - capability detection is not enough; auth fallback order must respect explicit credentials over ambient environment state
- Yangming check:
  - yes: a real execution defect was repaired in code and verified by successful remote publish, not just explained away
- Next improvement:
  - clean up repeated SSH host-key warnings and update build-index.py to remove the deprecated `datetime.utcnow()` usage

## 2026-03-21 — researched current GEO/SEO guidance for AI-visible websites

- Source files reviewed:
  - `https://developers.google.com/search/docs/fundamentals/creating-helpful-content`
  - `https://developers.google.com/search/docs/fundamentals/seo-starter-guide`
  - `https://developers.google.com/search/docs/crawling-indexing/robots/intro`
  - `https://developers.google.com/search/docs/appearance/structured-data/intro-structured-data`
  - `https://developers.openai.com/api/docs/bots`
- Hypothesis:
  - The highest-signal GEO best practices are still mostly strong SEO + explicit crawl/index accessibility + clear structure + answer-first content, not magical AI-only tricks.
- Action taken:
  - pulled current Google guidance on helpful content, SEO basics, robots, and structured data
  - pulled current OpenAI crawler guidance for OAI-SearchBot, GPTBot, and ChatGPT-User behavior
- Result / evidence:
  - confirmed that people-first originality, crawlability, structured data, and explicit AI crawler allowance remain the practical baseline
  - confirmed robots.txt should manage crawl, but noindex/password protection is required to reliably keep pages out of Google
  - confirmed OAI-SearchBot controls ChatGPT search visibility while GPTBot controls training use, and the settings are independent
- OpenClaw capability improved:
  - can now give more grounded GEO guidance tied to primary-source search and crawler docs instead of trend-chasing summaries
- Guardrail learned:
  - avoid treating GEO as separate from SEO fundamentals; AI-visible content still needs technical discoverability, trust signals, and content that directly satisfies intent
- Next improvement:
  - build a reusable GEO checklist and content-template pack for website publishing workflows

## 2026-03-21 — tightened mydoc publish hygiene by pinning host key and removing UTC deprecation

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc.sh`
  - `/Users/lu/.openclaw/workspace/my-doc-site/scripts/build-index.py`
- Hypothesis:
  - two noisy but real hygiene issues remained in the mydoc publish path: repeated host-key warnings from disposable known_hosts settings, and deprecated UTC timestamp generation in the index builder.
- Action taken:
  - updated `publish-mydoc.sh` to maintain a workspace-scoped known_hosts file via `ssh-keyscan` and use strict host checking against that file
  - updated `my-doc-site/scripts/build-index.py` to use `datetime.now(UTC)` and timezone-aware modified timestamps
- Result / evidence:
  - code paths were patched successfully
  - follow-up publish attempt failed for a different reason (`ssh: connect to host 172.245.114.89 port 22: Connection refused`), which indicates the prior warning/deprecation issues were no longer the active blocker on that run
- OpenClaw capability improved:
  - publish path is cleaner, safer, and future-proofed against Python UTC deprecation
- Guardrail learned:
  - cleanup fixes should still be verified separately from network availability; a clean transport config does not guarantee remote service uptime
- Next improvement:
  - when the VPS accepts SSH again, rerun publish to verify the host-key warning stays gone end-to-end

## 2026-03-21 — isolated flaky mydoc publish failure to scripted rsync transport while manual SSH/rsync still worked

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/scripts/publish-mydoc.sh`
  - `/Users/lu/.openclaw/workspace/my-doc-site/scripts/build-index.py`
- Hypothesis:
  - the remaining publish instability was not a stable VPS outage but a flaky scripted transport path, because basic TCP reachability and direct SSH sometimes succeeded while the wrapped publish command reported `Connection refused`.
- Action taken:
  - ran fresh reachability probes (`nc -zv 172.245.114.89 22`)
  - verified direct password SSH login with the workspace known_hosts file
  - ran the rsync command manually outside the wrapper and it succeeded
  - ran the remote site rebuild and verified the target public URL returned HTTP 200
- Result / evidence:
  - manual recovery path successfully published `docs/geo/website-geo-seo-publishing-checklist.md`
  - public URL is live despite wrapper-level intermittent failure
- OpenClaw capability improved:
  - we now have a verified fallback: when wrapper publish flakes, probe TCP, test direct SSH, run rsync manually, then rebuild remotely and verify HTTP
- Guardrail learned:
  - do not overgeneralize a single `Connection refused` from a wrapped command into “server is down” when lower-level probes can disprove it minutes later
- Next improvement:
  - instrument `publish-mydoc.sh` to log the fully expanded rsync/ssh transport and retry once after a short re-probe when the first connection is refused

## 2026-03-21 — studied karpathy/autoresearch as a pattern for bounded autonomous experimentation

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/workspace/docs/heartbeat-mirrors/AGENT_LEARNING_PROTOCOL.md`
  - `/Users/lu/.openclaw/workspace/docs/heartbeat-mirrors/LEARNING_REDESIGN.md`
  - `/Users/lu/.openclaw/workspace/docs/heartbeat-mirrors/agi-implementation.md`
  - `/Users/lu/.openclaw/workspace/docs/heartbeat-mirrors/agi-roadmap.md`
  - `/Users/lu/.openclaw/workspace/docs/heartbeat-mirrors/MEMORY_TREE.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
  - `https://raw.githubusercontent.com/karpathy/autoresearch/master/README.md`
  - `https://raw.githubusercontent.com/karpathy/autoresearch/master/program.md`
  - `https://raw.githubusercontent.com/karpathy/autoresearch/master/pyproject.toml`
- Hypothesis:
  - the highest-value idea in autoresearch is not “let agents run forever” but “constrain autonomous exploration to one mutable surface, one metric, one fixed budget, and a keep/discard loop”.
- Action taken:
  - extracted repo structure and control logic from README/program.md
  - compared it against OpenClaw doctrine around evidence-first execution, brain-vs-tool separation, and reclaiming control from execution surfaces
- Result / evidence:
  - confirmed autoresearch fits a strong bounded-loop pattern: single editable file, immutable evaluation harness, fixed 5-minute experiment budget, append-only results log, and branch advancement only on metric improvement
  - identified a direct OpenClaw application path: use the same control shape for prompt/program/protocol iteration rather than raw code-only research
- OpenClaw capability improved:
  - sharpened a reusable design principle for autonomous loops: isolate one mutation surface, lock the evaluator, cap runtime, and require explicit keep/discard promotion based on measured evidence
- Guardrail learned:
  - do not import the “NEVER STOP” rhetoric directly; OpenClaw should import the bounded experiment mechanics, not indefinite unsupervised momentum
- Yangming check:
  - yes: the study produced a concrete integration recommendation for OpenClaw control loops instead of a vague admiration note
- Next improvement:
  - write a short integration spec for an OpenClaw-style bounded autoresearch loop over prompts/protocols/docs with automatic rollback on no improvement
