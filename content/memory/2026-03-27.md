# 2026-03-27

- Heartbeat review (Friday weekly check) read required heartbeat sources and verified `./evals/benchmark-runner.sh` is still mock/simulated, so its outputs cannot be used as real benchmark evidence.
- Evidence: `benchmark-runner.sh` still contains explicit placeholders/comments: `Simulate test execution`, `TODO: Replace with actual OpenClaw agent invocation`, `Generate mock results`, and hard-coded aggregate/pass-rate values.
- Action taken: tightened `evals/master-dashboard.html` so it more explicitly labels itself as mock/static UI, adds a warning banner, changes the refresh button to `Refresh Mock Data`, and replaces misleading `Last run: X minutes ago` text with `Status: mock/static example only — no verified recent run`.
- Result: dashboard no longer presents recent-run language that could be mistaken for live telemetry, but the underlying benchmark runner defect remains open.
- OpenClaw capability improved: evidence labeling / fake-success prevention.
- Yangming check: doctrine was turned into a concrete file fix plus written evidence, not just observation.
- Next improvement: replace the simulated benchmark runner with a real measurement path before any future weekly benchmark claims or MEMORY.md trend updates.

## OpenClaw Sandbox Path Repair

- Diagnosed the recurring “OpenClaw forgets / cannot find doctrine or ACP context” complaint as a sandbox pathing defect, not an active Telegram transport failure.
- Root cause: autonomous prompts, agent `HEARTBEAT.md`, and core doctrine docs were still telling agents to read host-absolute paths such as `/Users/lu/.openclaw/...`, which sandboxed reads reject as `Path escapes sandbox root`.
- Repairs completed:
  - patched `/Users/lu/.openclaw/core/autonomous-loop.ts` to emit workspace-local doctrine paths
  - patched agent heartbeats and selected workspace `USER.md`/`MEMORY.md` references to use local paths
  - converted core doctrine docs to portable relative paths
  - added `/Users/lu/.openclaw/scripts/sync-agent-workspace-context.sh` and wired it into `/Users/lu/.openclaw/scripts/run-agent-safe.sh`
  - mirrored doctrine, memory-tree, learning, and intel files into each agent workspace
- Verification:
  - fresh direct `openclaw agent --agent architect ...` run completed successfully after the patch
  - `gateway.err.log` did not gain new `Path escapes sandbox root` lines during that fresh verification run
- Runtime follow-up:
  - discovered and fixed a separate autonomous-loop regression: missing `shouldSuppressTaskFromHotspot`
  - discovered and fixed `goal-manager-auto` workspace-sync alias handling in the new sync script
- Current live state:
  - gateway probe healthy
  - autonomous loop restarted on new PID and is progressing into real agent execution again
  - first `goal-manager-auto` task completed after the repair path, confirming the loop is no longer failing immediately on startup

## Daily self-review 2026-03-27 22:00 CST

- real progress with evidence:
  - repaired one concrete fake-success surface: `evals/master-dashboard.html` now explicitly presents benchmark data as mock/static instead of implying fresh live telemetry while `evals/benchmark-runner.sh` remains simulated.
  - repaired a real execution blocker in agent autonomy: sandbox-path doctrine failures were traced to host-absolute paths, then corrected across autonomous prompts, doctrine references, and agent workspace sync flow.
  - verification stayed concrete rather than narrative-only: a fresh `openclaw agent --agent architect ...` run succeeded after the path repair, `gateway.err.log` did not accumulate new `Path escapes sandbox root` errors in that verification window, and the autonomous loop resumed progressing into actual agent execution.
  - one follow-on regression and one sync edge case were also fixed the same day (`shouldSuppressTaskFromHotspot` and `goal-manager-auto` alias handling), which suggests the repair work stayed close to runtime reality instead of stopping at the first green-looking signal.
- scorecard (0-10):
  - execution quality: 8/10 — real defects were fixed and rechecked with same-day evidence, but the benchmark system is still not real and the autonomy repair still needs more than one-path validation.
  - memory quality: 8/10 — today’s record distinguishes root cause, repair, verification, and residual uncertainty fairly well; still could be tighter and more reusable instead of mostly chronological.
  - judgment: 9/10 — correctly separated dashboard honesty, sandbox path root cause, runtime verification, and residual risk rather than flattening everything into a generic “fixed” story.
  - honesty about evidence: 9/10 — did not treat mock benchmark surfaces as real, and did not overclaim the autonomy repair beyond the verification actually performed.
- concrete upgrades for tomorrow:
  1. add a stronger verification chain for the sandbox-path repair: confirm more than one agent/task path and check that autonomous runs keep succeeding over time, not just once.
  2. replace manual benchmark honesty checking with a first-class machine-readable evidence flag or artifact so Friday review cannot drift back into mock-as-real reporting.
  3. upstream or otherwise durable-ize the sandbox path / workspace sync doctrine so future prompts and agent workspaces cannot regress back to host-absolute references.
  4. compress today’s main lesson into durable doctrine only if it changes future behavior; keep `MEMORY.md` lean and avoid promoting raw chronology.
- anti-fake-success stance:
  - “dashboard now honest” is not the same as “benchmarks are real.”
  - “one fresh agent run succeeded” is not the same as “all autonomy pathing issues are closed.”
  - repairs should stay labeled by evidence depth: repaired surface, verified path, residual unknowns.
