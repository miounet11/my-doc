## 2026-03-12 Codex supervision pass (原始)

- Sources reviewed: HEARTBEAT.md, AGENT_LEARNING_PROTOCOL.md, LEARNING_REDESIGN.md, agi-implementation.md, agi-roadmap.md, MEMORY_TREE.md, CODEX_SUPERVISION_LOOP.md, AUTONOMY_PROTOCOL.md, and `projects/原始/data/retrospectives/codex-supervision-20260312-162505/summary.md`.
- Hypothesis: heartbeat should do one concrete evidence-first action instead of repeating stale status; Codex supervision is the highest-leverage safe action named directly by HEARTBEAT.md.
- Action taken: ran `./scripts/projectctl.sh supervise-codex 原始` and produced a fresh supervision retrospective at `projects/原始/data/retrospectives/codex-supervision-20260312-162505/summary.md`.
- Result/evidence: supervision completed successfully and emitted reviewer/world-model/goal-manager outputs plus structured summary paths.
- OpenClaw capability improved: supervision loop refreshed with current evidence instead of passive reminder repetition; heartbeat produced inspectable artifact.
- Wisdom/guardrail learned: if heartbeat direction is broad, choose the named protocol action that creates verifiable artifacts instead of narrating status.
- Yangming check: yes — doctrine became action via a concrete supervision run.
- Next improvement: consume reviewer/world-model/goal-manager outputs and turn the strongest reclaim recommendation into a file/prompt/protocol fix.

## 2026-03-12 Chat Mirror install hardening

- Source files reviewed: `projects/chat-mirror/server/package.json`, `projects/chat-mirror/web/package.json`, `projects/chat-mirror/server/src/index.ts`, `scripts/projectctl.sh`, `README.md`.
- Root cause confirmed: install failures were environment/runtime issues, not app logic bugs. `chat-mirror/server` depends on native addons (`better-sqlite3`, `bcrypt`), and local doctor evidence showed `Node v25.8.0` is outside the intended project range while macOS CLT presence must also be validated.
- Action taken: added package-level preinstall guards and engine ranges for server/web, created reusable `scripts/openclaw-node-doctor.py`, wired it into `projectctl doctor`, and standardized `chat-mirror` with `.nvmrc`, manifest, runbook, env example, and basic `dev/test/smoke` scripts.
- Result/evidence:
  - `./scripts/projectctl.sh doctor chat-mirror` now identifies the effective usable runtime (`node@22`) even while global `node` remains `v25.8.0`.
  - installed `node@24` and `node@22` via Homebrew; `node@24` still forced local `better-sqlite3` compilation and exposed that macOS CLT receipts are broken (`No Xcode or CLT version detected`), while `node@22` succeeded via prebuilt binary path.
  - `server` and `web` dependencies installed successfully with the LTS helper; both builds passed; host-run server returned `{"ok":true}` from `/health`.
- Guardrail learned: when host CLT is partially broken, the pragmatic unblock path for native Node addons may be to prefer an older supported LTS with working prebuilt artifacts, while separately tracking the damaged CLT installation as a host-level repair item.

## 2026-03-12 Chat Mirror acceptance evidence

- Added `projects/chat-mirror/scripts/acceptance.sh` plus `projects/chat-mirror/docs/ACCEPTANCE.md` so completion claims now require explicit acceptance evidence instead of install/build-only signals.
- Host-side acceptance run against `127.0.0.1:8791` persisted to `projects/chat-mirror/data/acceptance/latest.json`.
- Fresh evidence:
  - passed: health, wrong invite code rejection, register with `2026`, login, memory write, upload
  - failed: chat
  - manual/unverified: domain points to new system, no impact on original program, human acceptance
- Concrete blocker: chat still returns upstream `502` because the configured upstream token is invalid (`无效的令牌`), so the project is **not complete** and cannot be claimed accepted.
- Guardrail learned: for simple web/API projects, “build passed” must never be promoted to “done” unless an acceptance checklist shows all required user-facing flows and manual sign-off fields as satisfied.

## 2026-03-12 Chat Mirror acceptance recovery

- Action taken:
  - patched `chat-mirror/server` to fall back to local OpenClaw provider config for upstream `baseUrl/apiKey` when explicit env vars are missing
  - changed default local runtime posture from `0.0.0.0:8787` to `127.0.0.1:8791`
  - added `scripts/isolation-check.sh` to capture loopback listener + project-local data writes
- Evidence:
  - fresh acceptance run persisted to `projects/chat-mirror/data/acceptance/20260312T140953Z.json` and `latest.json`
  - automated acceptance items now all pass: register, invite code `2026`, login, chat, memory, upload
  - isolation evidence confirms only `node ... TCP 127.0.0.1:8791 (LISTEN)` and writes limited to `projects/chat-mirror/server/data/*`
- Current truth:
  - the project is stronger and the automatable acceptance surface is now green
  - it is **still not globally complete** because three non-automated items remain outside current evidence: domain points to new system, original program unaffected in real user-facing terms, and human acceptance

## 2026-03-12 Chat Mirror Agent Studio workbench landed

- Source files changed:
  - `projects/chat-mirror/server/src/index.ts`
  - `projects/chat-mirror/web/src/App.tsx`
  - `projects/chat-mirror/web/src/styles.css`
- Concrete action taken:
  - extended the session-agent binding endpoint so brand new sessions can bind an Agent before the first message is sent
  - rebuilt the `chat-mirror` frontend into an `Agent Studio` workbench with Agent selection, Agent creation, Skill creation, Skill-to-Agent binding, session-level Agent badges, and right-rail agent/knowledge coordination
  - preserved existing streaming chat, image/file/folder ingestion, session archive, and knowledge-link flows
  - deployed the new build to `chat.aibrowseratlas.com`
- Local evidence:
  - server build passed
  - web build passed
  - browser smoke on `http://127.0.0.1:8791` verified: create agent, auto-bind agent, create skill, auto-bind skill, send streaming message, save quick note into current session knowledge
  - browser console reported `0` errors during the smoke run
- Remote evidence:
  - `bash ./scripts/deploy.sh` rebuilt and restarted remote docker service at `96.44.158.158:/srv/chat-mirror`
  - `./scripts/acceptance.sh https://chat.aibrowseratlas.com` passed again after deployment and wrote fresh evidence to `projects/chat-mirror/data/acceptance/20260312T153800Z.json`
  - direct public browser check after registration confirmed the new `Agent Studio` workbench UI is live on `https://chat.aibrowseratlas.com`
- Guardrail learned:
  - Agent-style product upgrades need backend creation semantics to match the frontend draft flow; if a session can choose an Agent before its first message, the binding API must be able to materialize that session lazily instead of assuming it already exists.

## 2026-03-12 Chat Mirror auto-agent upgrade

- Product judgment:
  - Manus-style agent products should not force users to learn agent configuration first; the default path must be `chat first, orchestration behind the scenes`.
- New blueprint:
  - added `projects/chat-mirror/docs/AGENT_UPGRADE_BLUEPRINT.md` to define the full upgrade path: zero-learning-cost default UX, automatic routing, attachable knowledge, hidden advanced studio, background jobs, and future multi-channel execution.
- Concrete code changes:
  - backend now seeds a default capability pack when a user has no agents yet: `Atlas Auto Orchestrator`, `Atlas Knowledge Curator`, `Atlas Web App Analyst`, `Atlas Delivery Operator`
  - backend now auto-matches the first real user turn to an agent based on message, attachments, and linked knowledge, pins the session to that agent, and injects the agent + skills as hidden system context
  - frontend now defaults to automatic orchestration; manual agent override and authoring are moved behind `展开高级编排`
- Local evidence:
  - direct API smoke on `127.0.0.1:8791` showed first-turn auto routing of a web-analysis request to `Atlas Web App Analyst`, with both SSE `ready.agent` and persisted `sessions.agent_id` agreeing
- Remote evidence:
  - deployed to `https://chat.aibrowseratlas.com`
  - public API smoke also auto-routed a first-turn web-analysis request to `Atlas Web App Analyst`, and persisted `sessionAgentId` matched the streamed route result
  - full acceptance re-passed and wrote fresh evidence to `projects/chat-mirror/data/acceptance/20260312T155008Z.json`
- Guardrail learned:
  - if the product goal is low learning cost, manual configuration must become progressive disclosure, not the default interaction surface.
