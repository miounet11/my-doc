# 2026-03-08

## OpenClaw runtime repair

- Repaired `~/.openclaw/openclaw.json` to use `gateway.auth.mode=token` with loopback binding, then reinstalled and restarted the LaunchAgent so the live gateway matched config.
- Installed the OpenClaw Chrome extension files to `~/.openclaw/browser/chrome-extension` and verified the managed `openclaw` browser profile can start, navigate, and snapshot pages.
- Patched `~/.openclaw/scripts/run-agent-safe.sh` to stop falling back to `--local`; it now retries gateway mode only and cleans stale locks between attempts.
- Patched `~/.openclaw/scripts/cognitive-cron.sh` to use the bundled Node runtime when `bun` is unavailable; verified the script now completes and writes a fresh `report-2026-03-08.md`.
- Restarted AGI services with `~/.openclaw/scripts/start-agi.sh`; verified `autonomous-loop` and `learning-engine` are alive again and `~/.openclaw/state/world-state.json` advanced after successful task completions.
- Important lesson: adding `"type": "module"` to `~/.openclaw/package.json` broke `autonomous-loop.ts` and `learning-engine.ts` because both gate startup behind `require.main === module`. Reverted that change to restore the loop.

## Remaining manual step

- If the user wants OpenClaw to control existing daily Chrome tabs through the extension relay, Chrome still needs one manual setup step: open `chrome://extensions`, enable Developer Mode, load unpacked from `~/.openclaw/browser/chrome-extension`, then set the extension's Gateway token to match `gateway.auth.token`.

## Claude Code execution chain

- Verified that `scripts/cc-loop.sh` originally only generated briefs and ran checks; it did not call the `claude` CLI at all.
- Added `scripts/run-claude-brief.sh` and wired `scripts/projectctl.sh run-claude <project> <iteration> <role>` plus `projectctl cycle` to perform real Claude Code execution and persist outputs under `projects/<name>/data/claude-runs/iteration-<n>/`.
- Verified a minimal real Claude CLI call succeeds outside the Codex sandbox with `claude --print`.
- Verified a real project worker launch for `projects/原始` iteration `14` role `coder` now reaches Claude Code and produces a real `session_id`, but the configured upstream provider timed out on the full brief.
- Hardened the worker so upstream timeout/error text is treated as failure, with per-model attempt logs written to `*.attempts.json` instead of being mislabeled as success.
- Hardened `scripts/cc-loop.sh` so Claude worker failures are persisted into `loop-<n>.md` as `claude_run_status: failed` before the script exits non-zero. This prevents half-failed rounds from disappearing without a trace.
- Added `scripts/claude-execution-policy.json`, `scripts/claude-health.sh`, and `docs/claude-execution-protocol.md` so Claude execution is governed by a fixed policy and a documented state machine instead of ad hoc retries.
- Verified `scripts/projectctl.sh claude-health` returns `OK` for both `claude-opus-4.6` and `claude-sonnet-4.6` on minimal probes, which narrows the remaining problem to project-scale tool-using worker reliability rather than total provider outage.
- Added supervised Claude worker heartbeats and automatic retries: `run-claude-brief.sh` now writes `<role>.status.json` plus `*.heartbeat.jsonl`, retries transient failures per policy, and exposes live state through `scripts/projectctl.sh claude-status`.
- Verified the supervised path with a real `probe` worker on `projects/原始` iteration `14`; it produced a live heartbeat stream, a succeeded status file, and a real Claude `session_id`.
- Locked in a stability doctrine: keep the current main model fixed and improve the surrounding system instead of hiding defects with model swaps.
- Added `docs/OPENCLAW_STABLE_STATE_BLUEPRINT.md` plus `scripts/openclaw-readiness.sh` so the target state is explicit and operationally checkable.
- Readiness result on 2026-03-08: gateway `ok`, tailscale `ok`, tailscale serve `ok`, Claude probe `degraded`. This confirms the system bottleneck remains provider reliability, not gateway/network/runtime health.

## Autonomous-loop recovery hardening

- Hardened `~/.openclaw/scripts/start-agi.sh` so autonomous runtime now defaults to lower pressure: `OPENCLAW_AUTONOMOUS_PARALLELISM=1`, `OPENCLAW_IGNITE_CONCURRENCY=1`, and `OPENCLAW_LOOP_INTERVAL_MS=60000`.
- Added `~/.openclaw/scripts/watch-agi.sh`, a watchdog that:
  - removes stale session locks whose owning pid is gone
  - detects recent timeout storms from `gateway.err.log`
  - detects repeated `Request was aborted` and `All models failed`
  - detects missing autonomous-loop / learning-engine processes
  - triggers bounded recovery with cooldown instead of uncontrolled restart loops
- The watchdog now restarts the gateway first, and if `openclaw gateway restart` fails because the LaunchAgent is missing, it falls back to `openclaw gateway install --force` + `openclaw gateway start`.
- Hardened `~/.openclaw/scripts/run-agent-safe.sh` so retryable failures now include `Request was aborted`, `All models failed`, and `embedded run timeout`, with 4 attempts and incremental backoff.
- Verified by direct trace that the watchdog no longer crashes on macOS shell differences; the first version failed because `date -Is` and a `tail | python` pipeline were Linux-biased, and both issues were patched.
- Restored the gateway LaunchAgent with `openclaw gateway install --force` and `openclaw gateway start`; `openclaw gateway status` is back to `Runtime: running` and `RPC probe: ok`.
- Verified the final live runtime after restart:
  - autonomous loop pid present
  - learning engine pid present
  - watchdog pid present
  - gateway service loaded and reachable on loopback
- Operational lesson: if the same 180s timeout repeats across multiple autonomous agents, do not only raise timeout; first reduce concurrency, then add recovery based on observed timeout/lock patterns.

## Model routing change

- Updated live `~/.openclaw/openclaw.json` so both `agents.defaults.model` and the explicit `main` agent route now use:
  - primary: `openai/gpt-5.4`
  - fallback: `openai/grok-420-agents`
- Added `gpt-5.4` to the configured `models.providers.openai.models` list so the new route points at a declared model id instead of an undeclared string.
- Validated config with `openclaw config validate` and reloaded the gateway with `openclaw gateway restart`.

## Model routing change v2

- Refined live model routing again in `~/.openclaw/openclaw.json`:
  - `main` agent:
    - primary: `openai/gpt-5.4`
    - fallbacks: `openai/grok-420-agents`, `openai/minimax-m2.1`
  - `agents.defaults`:
    - primary: `openai/minimax-m2.1`
    - fallbacks: `openai/deepseek-3.2`, `openai/auto`
- Added `minimax-m2.1` and `deepseek-3.2` to the declared provider model catalog so both routes resolve to known ids.
- Revalidated config and restarted the gateway after the routing update.

## Agent routing rollback

- Rolled back `agents.defaults.model` away from `minimax-m2.1` after repeated malformed-request failures on the live provider path.
- New agent default route is:
  - primary: `openai/grok-420-agents`
  - fallback: `openai/gpt-5.4`
  - final fallback: `openai/auto`
- Reason: `minimax-m2.1` produced repeated `400 {"message":"Improperly formed request.","reason":null}` responses across multiple OpenClaw agents, which indicates provider/request-shape incompatibility rather than ordinary timeout instability.

## Iteration 15 kickoff

- Started a new OpenClaw cycle for `projects/原始` with `scripts/projectctl.sh cycle 原始 15`.
- Verification gates passed before Claude execution:
  - test suite: `16/16 OK`
  - smoke: `ok`
  - audit payload reported healthy project assets
- Claude squad iteration 15 briefs were generated under `projects/原始/docs/claude-squad-iteration-15/`.
- Real Claude worker execution began for role `architect`; heartbeat and status files were created under `projects/原始/data/claude-runs/iteration-15/`.
- Observed supervised retry in action:
  - first `architect` attempt on `claude-opus-4.6` timed out around 240s
  - worker entered bounded backoff and automatically started retry 2
- Important operational proof: the OpenClaw -> Claude worker loop is now not just “brief generation”; it is producing live worker status, heartbeats, and policy-driven retry behavior during a real project cycle.

## False-success correction wave

- Iteration 15 initially looked successful at the wrapper level, but all four Claude result bodies were actually `[Internal Error] HTTP client getter not set`.
- Hardened `scripts/claude-execution-policy.json` and `scripts/run-claude-brief.sh` so these internal-error bodies are now treated as `model_error`, not success.
- Corrected `projects/原始/data/retrospectives/loop-15.md` and `loop-15-reflection.md` to mark the round as `invalid_output_detected` instead of success.
- Found the same pattern on OpenClaw supervision agents: `reviewer` / `world-model` could emit `400 {"message":"Improperly formed request."}` while still carrying `status: ok`.
- Hardened live `~/.openclaw/scripts/run-agent-safe.sh` to reject malformed successful-looking payloads and retry them.
- Hardened `scripts/summarize-supervision.py` so invalid payload text is surfaced as `status: invalid output`.
- Added `OPENCLAW_SUPERVISION_FORCE=1` support to `scripts/openclaw-supervise-codex.sh` so fresh supervision can bypass recent-summary reuse after a detector fix.
- Launched a fresh forced supervision pass after these fixes to regenerate supervision artifacts under the stricter validity rules.
- Recomputed the fresh supervision summary and confirmed all three agents were correctly surfaced as `invalid output` rather than `pending`.
- Started iteration 16 immediately after the correction wave so the stricter validators are exercised on the next real project cycle instead of waiting for a later session.
