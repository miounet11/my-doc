## 2026-03-26
- Heartbeat 2026-03-26 09:29 CST:
  - sources reviewed: `/Users/lu/.openclaw/workspace/HEARTBEAT.md`, all required heartbeat mirror docs, `/Users/lu/.openclaw/workspace/docs/context-control/CONTEXT_CONTROL_PROTOCOL.md`, recent memory for 2026-03-25; `memory/2026-03-26.md` did not exist yet.
  - verified-now defect: the weekly quality surface still contains fake evidence. `evals/benchmark-runner.sh` explicitly says `# Simulate test execution (replace with actual test runner)` and writes fixed mock pass-rate/token numbers, while `evals/master-dashboard.html` still claims `Real-time system metrics` and uses placeholder UI text like `Dashboard refreshed! In production, this would fetch latest metrics from evals/results.json` plus hard-coded “Last run: X minutes ago”.
  - action taken: repaired the dashboard copy so it no longer presents the benchmark surface as real-time/live evidence when the underlying benchmark runner is still mock-data based.
  - capability improved: evidence labeling and anti-fake-success discipline in the heartbeat quality/reporting path.
  - yangming check: doctrine from HEARTBEAT.md and AGENT_LEARNING_PROTOCOL.md became an actual user-facing surface repair, not just a note.
  - next improvement: replace the mock benchmark runner with a real evaluator or wire the dashboard to only display generated artifacts from `evals/results/` with explicit freshness metadata.
- Heartbeat 2026-03-26 10:59 CST:
  - sources reviewed: re-read `/Users/lu/.openclaw/workspace/HEARTBEAT.md`, required heartbeat docs, `evals/benchmark-runner.sh`, `evals/master-dashboard.html`, `evals/test-cases.json`, `memory/optimizations.md`, and today’s memory entry.
  - verified-now defect: even after the dashboard labeling repair, the heartbeat protocol itself still allowed Friday review flow to treat `./evals/benchmark-runner.sh` as if it were an executable source of real benchmark evidence. Direct file evidence shows the runner still contains `Simulate test execution`, `TODO: Replace with actual OpenClaw agent invocation`, and fixed aggregate numbers.
  - action taken: tightened `HEARTBEAT.md` so weekly review must first verify whether the benchmark runner is still mock/simulated, must not treat mock output as real benchmark evidence, and must keep dashboard/MEMORY updates explicitly labeled as mock vs real.
  - capability improved: heartbeat protocol now guards against a second-order fake-success path where honest dashboard labels coexist with misleading weekly review instructions.
  - yangming check: the earlier diagnosis was promoted into executable protocol, not left as a one-off observation.
  - next improvement: replace the mock benchmark runner with a real evaluator that emits inspectable artifacts under `evals/results/`, then wire dashboard freshness and weekly MEMORY trends to those artifacts only.
- Runtime health check 2026-03-26 11:28 CST:
  - verified-now: core OpenClaw runtime is up. `openclaw status`, `openclaw gateway status`, and `~/.openclaw/scripts/verify-stability.sh` all passed. Direct smoke test `openclaw agent --agent main --message 'Reply with OK only.' --json` returned `OK` in about 4.6s.
  - verified-now defect: Telegram ingress is actively unhealthy even though `openclaw status` still reports Telegram `OK`. Fresh repeated errors in `/Users/lu/.openclaw/logs/gateway.err.log` and `/tmp/openclaw/openclaw-2026-03-26.log` show `Polling stall detected`, `Polling runner stop timed out`, and forced restart cycles.
  - verified-now defect: `openclaw status --deep` timed out with `gateway timeout after 10000ms`, so the gateway is reachable but not reliably responsive under heavier inspection.
  - verified-now defect: `ai.openclaw.daily-backup` remains broken with exit `127`; `/Users/lu/.openclaw/logs/daily-backup.out` still shows `openclaw: command not found` and `env: node: No such file or directory`.
  - verified-now: the `mydoc` deploy retry job is not crashing. `ai.openclaw.mydoc-deploy` has last exit code `0`, and `/Users/lu/.openclaw/logs/mydoc-deploy-retry.log` shows repeated `skip reason=up-to-date`, which indicates idle/no new deploy work rather than a broken loop.
  - verified-now: no evidence of local gateway port conflict on 18789. `lsof -nP -iTCP:18789 -sTCP:LISTEN` shows only the OpenClaw node process listening on loopback.
- Runtime repair 2026-03-26 11:50 CST:
  - action taken: patched `/Users/lu/.openclaw/scripts/daily-backup.sh` so launchd runs with an explicit Homebrew-aware `PATH` and can invoke OpenClaw through `/opt/homebrew/opt/node/bin/node /opt/homebrew/lib/node_modules/openclaw/dist/entry.js` instead of relying on the fragile `#!/usr/bin/env node` chain.
  - action taken: patched `/Users/lu/.openclaw/openclaw.json` to add `channels.telegram.network.autoSelectFamily=false` and `channels.telegram.network.dnsResultOrder=ipv4first`, then restarted the gateway.
  - verified-now: direct agent path still works after repair. `openclaw agent --agent main --message 'Reply with OK only.' --json` returned `OK` in about 5.8s.
  - verified-now: Telegram stall loop cleared after the config reload at about `11:45 CST`. Log scan across `11:45` through `11:50` found `post_fix_error_count=0` for `Polling stall`, `getUpdates conflict`, and `UND_ERR_CONNECT_TIMEOUT`.
  - verified-now: manual backup run completed successfully and created `/Users/lu/Backups/openclaw/2026-03-26T03-45-17.510Z-openclaw-backup.tar.gz`.
  - verified-now: the LaunchAgent-driven backup path is fixed end-to-end. Child process inspection showed `PATH=/opt/homebrew/opt/node/bin:/opt/homebrew/bin:...`, and the LaunchAgent later finished with `last exit code = 0`, writing `/Users/lu/Backups/openclaw/2026-03-26T03-48-12.158Z-openclaw-backup.tar.gz`.
  - residual diagnostic issue: `openclaw status --deep` still fails at the default `--timeout 10000`, but succeeds with `--timeout 30000`; this indicates a slow deep probe path rather than a dead gateway. The deep output also contains an inconsistent Telegram health row (`OFF not configured`) despite the Telegram channel being loaded and stable, so there is likely a built-in status/reporting bug remaining in OpenClaw 2026.3.24.
- Runtime repair 2026-03-26 12:08 CST:
  - action taken: patched installed OpenClaw status surfaces at `/opt/homebrew/lib/node_modules/openclaw/dist/status-DUu4C8tF.js` and `/opt/homebrew/lib/node_modules/openclaw/dist/status-json-Jwr2buEe.js` so `status --deep` passes an explicit internal health probe timeout plus an extra RPC slack margin instead of reusing the exact outer CLI deadline.
  - verified-now root cause: default deep health was genuinely sitting on the timeout edge. Five pre-fix back-to-back runs produced `10018`, `8086`, `6996`, `10006`, and `10007` ms health durations, which explains the intermittent `gateway timeout after 10000ms` failures.
  - verified-now: after the timeout-margin patch, five consecutive default `openclaw status --deep --json` runs all completed successfully with health durations `6391`, `7356`, `10005`, `7683`, and `6998` ms. The `10005ms` success confirms the self-timeout defect is repaired rather than just hidden by a fast sample.
  - verified-now: human-readable `openclaw status --deep` now renders Telegram as `OK` and no longer flips back to the false `OFF / not configured` summary on current runs.
  - verified-now: direct interaction path is healthy after the repair. `openclaw agent --agent main --message 'Reply with OK only.' --json` returned `OK` in about `6.5s`.
  - residual issue: Telegram transport is improved but not perfectly clean. Fresh runtime logs still show one later network fallback event at `2026-03-26 11:57:06 CST` (`UND_ERR_CONNECT_TIMEOUT` -> sticky IPv4 fallback), but there is still no renewed evidence of the earlier `Polling stall` / `getUpdates conflict` restart storm after the main config fix.
  - operational note: these status fixes are currently hot patches against the installed Homebrew/OpenClaw dist, so they should be upstreamed or re-applied after package upgrades.
- Runtime durability 2026-03-26 12:19 CST:
  - action taken: added `/Users/lu/.openclaw/workspace/scripts/reapply-openclaw-runtime-patches.sh`, a version-gated (`2026.3.24`) workspace repair script that re-applies the current known dist hot patches for `status-DUu4C8tF.js`, `status-json-Jwr2buEe.js`, `channel-summary-BI4Ows9P.js`, `gateway-cli-SPSnwPDk.js`, and `health-BKCofxn3.js`.
  - verified-now: running the script on the current machine reported all target files `already-patched`, then completed a deep-health verification with `durationMs=7416`, `telegramConfigured=true`, and `telegramAccountId=architect`.
  - verified-now: the script is safe to use as an operator recovery path after Homebrew/OpenClaw reinstalls on the same runtime version because it refuses to patch any version other than `2026.3.24`.
  - residual note: this is a local operator reapply path, not a true upstream fix. Future OpenClaw upgrades still need either an upstream patch or a new version-specific repair script.
- Daily self-review 2026-03-26 22:00 CST:
  - real progress with evidence:
    - repaired a fake-evidence surface instead of merely noting it: `evals/master-dashboard.html` no longer presents mock benchmark output as real-time/live telemetry while `evals/benchmark-runner.sh` still contains simulated/fixed numbers.
    - promoted the benchmark honesty lesson into doctrine: `HEARTBEAT.md` now explicitly requires Friday review to verify whether the benchmark runner is still mock/simulated before treating any output as real evidence, and requires dashboard/MEMORY updates to stay labeled mock vs real.
    - repaired two runtime defects end-to-end with fresh verification: the daily backup path now works under launchd and produced fresh archives; Telegram polling stall/restart loops stopped after the network preference config change and gateway restart.
    - repaired a status-surface self-timeout bug with measured evidence: pre-fix default `openclaw status --deep --json` runs intermittently failed around the 10s edge (`10018`, `10006`, `10007` ms), while post-fix runs completed successfully even at `10005ms`, showing the problem was the timeout wiring rather than a dead gateway.
    - added an operator durability path instead of leaving hand patches undocumented: `scripts/reapply-openclaw-runtime-patches.sh` can safely reapply the current known runtime hot patches for OpenClaw `2026.3.24` and refuses mismatched versions.
  - scorecard (0-10):
    - execution quality: 8/10 — multiple real defects were fixed and rechecked with direct evidence, but several repairs are still local hot patches rather than upstreamed solutions, and Telegram transport still showed at least one later fallback warning.
    - memory quality: 8/10 — today’s facts were recorded with clearer verified-now vs residual-state separation, and one lesson was promoted from chronology into protocol; still too much of the day lives as long narrative rather than compact reusable doctrine.
    - judgment: 9/10 — good separation between core runtime health, ingress health, status-reporting correctness, and benchmark evidence validity. Mixed states stayed mixed instead of being flattened into a fake green result.
    - honesty about evidence: 9/10 — strong today. Kept distinguishing local/operator repairs from true upstream fixes, and did not claim benchmark or deploy surfaces were real when the underlying evidence stayed mock or local-only.
  - concrete upgrades for tomorrow:
    1. upstream or codify the runtime/status fixes properly so the system stops depending on dist hot patches and version-gated reapply scripts.
    2. add a first-class evidence surface for benchmark reality: one command or artifact path that says unambiguously whether `evals/benchmark-runner.sh` is mock, hybrid, or real, so weekly review does not rely on manual file inspection.
    3. investigate the remaining Telegram fallback/noise path and decide whether it is harmless degraded networking or a precursor signal for future polling instability.
    4. compress repeated runtime facts into durable doctrine only where they will change future behavior; avoid turning `memory/2026-03-26.md` into a second archive.
  - anti-fake-success rule reinforced: local patchability, cleaner status output, and stopped restart storms do not equal upstream resolution; mock benchmark runners and local-only repairs stay explicitly non-final until the evidence surface itself becomes real and durable.
