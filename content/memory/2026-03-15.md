# 2026-03-15

## new2005-finance content-factory kickoff is now evidence-backed through live ACP execution

- Source files reviewed:
  - `/Users/lu/.openclaw/workspace/HEARTBEAT.md`
  - `/Users/lu/.openclaw/docs/AGENT_LEARNING_PROTOCOL.md`
  - `/Users/lu/.openclaw/LEARNING_REDESIGN.md`
  - `/Users/lu/.openclaw/docs/agi-implementation.md`
  - `/Users/lu/.openclaw/docs/agi-roadmap.md`
  - `/Users/lu/.openclaw/memory-tree/core/MEMORY_TREE.md`
  - `/Users/lu/.openclaw/workspace/docs/CODEX_SUPERVISION_LOOP.md`
  - `/Users/lu/.openclaw/workspace/docs/AUTONOMY_PROTOCOL.md`
- Hypothesis:
  - the highest-leverage next action for the new `new2005-finance` push is not more strategy text, but a real ACP Codex execution that starts building the structured-data content factory MVP inside the project.
- Action taken:
  - routed the user's “火力全开” Codex request through the ACP path and attempted a persistent thread-bound Codex session first.
  - observed a real platform constraint: ACP thread binding is not supported for this Telegram context, so thread-mode failed with concrete runtime errors instead of being silently hand-waved.
  - adapted immediately by switching to `sessions_spawn(runtime="acp", agentId="codex", mode="run")` against `/Users/lu/.openclaw/workspace/projects/new2005-finance` with a bounded implementation brief covering schema, seed data, docs, exporter/validator, and a minimal API/page surface.
  - monitored streamed progress from the Codex run, which showed concrete repo exploration and implementation steps: reading session guidance, mapping the Next.js app shape, settling the schema/integration plan, writing raw seed data, and wiring a shared catalog builder plus minimal integration surface.
- Result/evidence:
  - ACP dispatch succeeded in one-shot run mode with child session `agent:codex:acp:362ec910-a779-41b2-bdd6-4ff0e4808867` and run id `1f927c3b-1a76-4c9a-a3f3-f27411ee4f79`.
  - runtime evidence is explicit that the Codex worker is actively executing inside the target repo rather than being described hypothetically.
  - the failure mode is also explicit: Telegram currently does not support ACP thread spawn here, so `mode="session" + thread=true` cannot be claimed as available in this surface.
- OpenClaw capability improved:
  - strengthened ACP execution honesty by converting a user request for “multiple Codex / acpx 火力全开” into a valid ACP contract with real runtime evidence, while preserving the distinction between unsupported persistent-thread ACP and supported one-shot ACP runs.
- Wisdom/guardrail learned:
  - ACP escalation discipline matters under pressure: when the ideal execution surface is unavailable, the correct move is not to narrate around it, but to degrade explicitly to a still-valid contract and keep real work moving.
- Yangming check:
  - yes — instead of promising future multi-agent action, the session produced a live Codex execution surface with inspectable progress updates and named runtime identifiers.
- Next improvement:
  - when this Codex run finishes, extract the changed files and verification steps into both project docs and durable memory, then decide whether to launch a second bounded Codex run for the prompt/generator layer rather than mixing schema work and content-generation logic in one uncontrolled pass.

## ACPX routing rule clarified for future multi-step Codex work

- User decision:
  - future handling should standardize on the already-confirmed split: writable ACPX execution is available, but Telegram-bound persistent ACP thread continuation is not yet verified/supported.
- Operational rule:
  - do not diagnose Telegram follow-up stalls as a generic ACP write-permission problem unless a fresh write failure is actually observed.
  - for Telegram-origin heavy coding work, default to bounded ACP `mode="run"` dispatches that can complete real repo writes inside the workspace.
  - if true continuous multi-step Codex work is needed, route it to a host-side or otherwise session-capable ACP/acpx surface instead of claiming Telegram thread binding exists.
- Guardrail:
  - keep separating `write access available` from `thread/session binding unsupported`; conflating them would recreate false blocker narratives and wrong fixes.

## ACP surface-aware routing helper and Telegram ACP audit parser corrected

- Action taken:
  - added a surface-aware ACP recommendation mode to `scripts/openclaw-acp-dispatch.sh`, exposed through `scripts/projectctl.sh acp-dispatch recommend ...`.
  - this helper now downgrades Telegram `prefer-session` requests into bounded `mode="run"` descriptors with an explicit reason, while allowing real `mode="session"` descriptors on host/session-capable surfaces.
  - updated Telegram ACP watch/audit outputs so they now recommend bounded Telegram runs plus host-side escalation for persistent multi-turn work instead of vaguely waiting on thread binding.
  - fixed a real evidence bug in `scripts/openclaw-telegram-acp-audit.sh` and `scripts/openclaw-capability-audit.sh`: the old parser assumed a flat `channels.telegram.threadBindings` shape and falsely reported config misalignment, while the live config actually stores thread-binding settings per Telegram account under `channels.telegram.accounts.*`.
- Verified result:
  - `bash scripts/openclaw-acp-dispatch.sh recommend telegram codex "Implement feature X" --prefer-session --cwd /Users/lu/.openclaw/workspace/projects/new2005-finance` now returns `decision="bounded-run-fallback"` with `mode="run"`.
  - the same helper on `host` returns `decision="persistent-session"` with `mode="session"` and `thread=true`.
  - `python3 scripts/openclaw-telegram-acp-audit.sh` now reports `config_ready=true`, `thread_bindings_enabled=true`, and `spawn_acp_sessions=true`; the remaining gap is correctly narrowed to `thread_spawn_verified=false` / closure unverified rather than a false config-disable diagnosis.

## ACPX full-permission host fix is now applied and evidence-backed

- Root cause found:
  - gateway logs contained explicit runtime failures: `AcpRuntimeError: Permission denied by ACP runtime (acpx). ACPX blocked a write/exec permission request in a non-interactive session. Configure plugins.entries.acpx.config.permissionMode ...`
  - this proved the remaining blocker was not generic Telegram continuity alone; ACPX itself was still on a restricted non-interactive permission posture.
- Host fix applied:
  - set `plugins.entries.acpx.config.permissionMode = approve-all`
  - set `plugins.entries.acpx.config.nonInteractivePermissions = fail`
  - restarted the host OpenClaw gateway successfully
- Post-fix evidence:
  - `openclaw config get plugins.entries.acpx.config.permissionMode` now returns `approve-all`
  - `openclaw config get plugins.entries.acpx.config.nonInteractivePermissions` now returns `fail`
  - gateway restart succeeded and came back `RPC probe: ok`
  - a real host-side ACPX smoke run dispatched Codex child session `agent:codex:acp:0df27739-bcf0-4371-be94-92441118390f` with run id `f57d93d8-5a25-4e06-9c4f-9faccc77e7d6`
  - direct file verification confirmed `/Users/lu/.openclaw/workspace/data/system/acpx-write-smoke.txt` exists with content `ACPX write smoke ok`
- Follow-up hardening:
  - `scripts/openclaw-readiness.sh` now reports `acpx_permission`, `acpx_permission_mode`, and `acpx_non_interactive_permissions`
  - `scripts/openclaw-capability-audit.sh` now includes ACPX permission posture in `acp_orchestration`
  - `scripts/openclaw-telegram-acp-audit.sh` now records whether ACPX full permissions are enabled
  - `docs/ACP_ACPX_INTEGRATION_PROTOCOL.md` now treats ACPX permission policy as a first-class preflight item and adds `permission-blocked` as a failure class

## Telegram ACP closure gap is now narrowed to a structural conversation-type limit

- Evidence found in the installed OpenClaw build:
  - Telegram thread binding is implemented, but bindable Telegram conversation ids are only:
    - direct-message chat ids, or
    - group forum topics in the form `<chatId>:topic:<threadId>`
  - for plain Telegram groups without `MessageThreadId`, the runtime returns no bindable conversation id, so ACP `/focus` or thread-bound ACP spawn cannot close there.
- Current live state:
  - the active Telegram surface is `agent:main:telegram:group:-5119481739`
  - `chat_type = group`
  - `thread_id = null`
  - therefore this conversation is a plain non-topic group and is not bindable for Telegram ACP thread/session continuity.
- Tooling updated:
  - `scripts/openclaw-telegram-acp-audit.sh` now exposes `live_conversation_bindable` and `live_conversation_reason`
  - `scripts/openclaw-telegram-acp-watch.sh` now changes `next_step` to the concrete recommendation when the live Telegram surface is a plain non-topic group
  - `scripts/openclaw-capability-audit.sh` now carries that bindability diagnosis into IM-channel gaps
- Operational consequence:
  - there is no further local config-only repair for persistent ACP thread binding on this exact Telegram group surface
  - persistent Telegram ACP continuity now requires one of:
    - move the workflow to a Telegram DM
    - use a Telegram forum topic / supergroup topic
    - keep this group on bounded `mode="run"` and use host-side acpx sessions for durable multi-step work
